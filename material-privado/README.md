# 🔒 Material privado (solo para el profe)

Esta carpeta **no se publica** en el sitio. MkDocs solo construye `docs/`, y la futura migración a
Astro solo construirá `src/content/docs/`, así que todo lo que viva acá queda fuera del sitio y del
buscador — igual que `guiones/`.

Sirve para material teacher-only: borradores, versiones archivadas, notas internas.

## Contenido

- `cronograma-original.md` — el cronograma detallado semana-a-semana tal como estaba antes de pasar
  al modelo liviano (hitos + bitácora). Se conserva para comparar **lo planeado vs. el atraso real**.
- `MIGRACION-ASTRO.md` — guía para el re-autorado completo del curso a la plataforma interactiva
  (se completa durante el POC).

> ⚠️ No mover archivos de esta carpeta a `docs/` ni a `src/content/docs/` sin querer publicarlos.
