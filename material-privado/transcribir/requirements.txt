# Transcripción local de las entrevistas.
#
#     pip install -r material-privado/transcribir/requirements.txt
#
# Todo corre en esta máquina: faster-whisper baja el modelo una vez y no manda
# nada a ningún servidor. Los audios son gente del CFP hablando de su trabajo,
# así que eso no es un detalle.
#
# NO hace falta instalar ffmpeg aparte: faster-whisper 1.x decodifica con PyAV,
# que se instala como dependencia y trae las librerías de ffmpeg adentro.
# Verificado abriendo un .webm/opus con ffmpeg NO instalado en el sistema.
#
# ⚠️ En esta máquina hay dos Python 3.14 distintos. Usar `python`, no `python3`,
# o va a decir que falta el módulo.
faster-whisper==1.2.0
