---
title: La función print()
description: "Lo primero que hace todo programa: mostrar algo. Comillas, comas, saltos de línea y los errores clásicos."
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_1_fundamentos/funcion_print/" />

## El problema

Una computadora puede hacer millones de cuentas por segundo. Pero si no te **muestra** el resultado,
es como si no hubiera hecho nada: el número queda adentro de la máquina y se pierde.

`print()` es la forma de decirle *"esto, mostrámelo"*.

```python
print("Hola mundo")
```

## Comillas: texto vs. otra cosa

Esta es la primera trampa del lenguaje.

```python
print("Hola")    # muestra: Hola
print(Hola)      # 💥 error: NameError
```

Con comillas, `"Hola"` es un **texto** (un *string*). Sin comillas, Python cree que `Hola` es el
**nombre de algo** que vos definiste antes — y como no existe, se queja.

Sirven las comillas dobles `"` o simples `'`, mientras abras y cierres con la misma.

<EjercicioPython
  titulo="Tu primer mensaje"
  dificultad="🌱"
  starter={`print("Hola")`}
  tests={`assert salida.strip() == "Hola mundo", f'tu programa imprimió {salida.strip()!r} y esperaba "Hola mundo"'`}
>
Cambiá el texto para que el programa imprima exactamente `Hola mundo`.

Ojo con las mayúsculas y el espacio: el test compara **caracter por caracter**.
</EjercicioPython>

## Mostrar varias cosas juntas

`print()` acepta varias cosas separadas por **comas**, y las muestra con un espacio en el medio:

```python
print("Tengo", 30, "años")   # Tengo 30 años
```

Fijate que `30` va **sin comillas**: es un número, no un texto. `print()` se banca ambos.

:::caution[La coma no es lo mismo que el `+`]
```python
print("Tengo", 30)     # ✅ Tengo 30
print("Tengo" + 30)    # 💥 TypeError: no podés sumar texto con número
```
La coma **junta cosas para mostrarlas**. El `+` intenta **sumarlas**, y sumar un texto con un número
no significa nada.
:::

<EjercicioPython
  titulo="Una ficha de datos"
  dificultad="🌱"
  datos={`nombre = "Ana"\nedad = 30`}
  starter={`# 'nombre' y 'edad' ya tienen valor. Mostrá la frase con print().\n`}
  tests={`assert correr(nombre="Ana", edad=30).strip() == "Ana tiene 30 años", f'imprimiste {correr(nombre="Ana", edad=30).strip()!r}'\nassert correr(nombre="Beto", edad=7).strip() == "Beto tiene 7 años", "probá cambiando los valores: la frase tiene que acompañar"`}
  pistas={[
    "Escribí a mano el resultado para Ana: <code>Ana tiene 30 años</code>. ¿Qué partes cambian según quién sea, y qué partes son siempre iguales?",
    "¿Cómo meterías el valor de una variable <em>adentro</em> de un texto que ya escribiste, sin cortarlo en pedazos?",
  ]}
>
Mostrá una frase como `Ana tiene 30 años`, usando las variables `nombre` y `edad`.

El test también prueba con `Beto` y `7`, así que la frase tiene que armarse **a partir de las
variables**, no estar escrita a mano.
</EjercicioPython>

## f-strings: meter valores adentro del texto

Cuando el texto y los datos se mezclan mucho, las comas se vuelven incómodas. Para eso están las
**f-strings**: una `f` antes de la comilla, y las variables entre llaves `{}`.

```python
nombre = "Ana"
edad = 30

print(f"{nombre} tiene {edad} años")   # Ana tiene 30 años
```

Se lee de corrido, que es justamente la gracia.

:::note[Si te olvidás la `f`]
```python
print("{nombre} tiene {edad} años")   # {nombre} tiene {edad} años
```
Sin la `f`, las llaves son texto común y salen tal cual. Es un error silencioso: no rompe, solo
muestra cualquier cosa. Si ves llaves en tu salida, fijate la `f`.
:::

## Cambiar el separador: `sep`

Por defecto `print()` separa con un espacio, pero podés elegir con qué:

```python
print("Python", "es", "genial")             # Python es genial
print("Python", "es", "genial", sep="-")    # Python-es-genial
print(1, 2, 3, sep=",")                     # 1,2,3
print("2026", "08", "05", sep="/")          # 2026/08/05
```

`sep` va **al final**, después de todo lo que querés mostrar, y se aplica entre cada par de valores
(no al principio ni al final).

## Saltos de línea

Cada `print()` termina con un salto de línea. Si querés uno en el medio, usás `\n`:

```python
print("Primera línea\nSegunda línea")
```

Y si NO querés el salto al final, `end=""`:

```python
print("Hola, ", end="")
print("mundo")            # Hola, mundo  (en una sola línea)
```

<EjercicioPython
  titulo="Un cartel de tres líneas"
  dificultad="🌿"
  datos={`titulo = "OFERTA"`}
  starter={`# 'titulo' ya tiene valor. Mostrá el cartel de tres líneas.\n`}
  tests={`assert correr(titulo="OFERTA").strip() == "===\\nOFERTA\\n===", f'imprimiste {correr(titulo="OFERTA").strip()!r}'\nassert correr(titulo="HOLA").strip() == "===\\nHOLA\\n===", "con otro título el cartel tiene que acompañar"`}
  pistas={[
    "Escribí en papel las tres líneas que tienen que salir. ¿Qué caracter separa una línea de la siguiente cuando todo va en un solo texto?",
    "Las líneas de arriba y abajo son siempre iguales sin importar el título. ¿Qué parte del texto depende del parámetro y cuál no?",
  ]}
>
Mostrá el `titulo` entre dos líneas de `===`.

Con `titulo = "OFERTA"` tiene que verse así:

```
===
OFERTA
===
```
</EjercicioPython>

<EjercicioPython
  titulo="Desafío: la ruta de lenguajes"
  dificultad="🌳"
  datos={`primero = "Python"\nsegundo = "Java"\ntercero = "C++"\ncuarto = "JavaScript"`}
  starter={`# Los cuatro lenguajes ya tienen valor.\n# Mostrá la ruta en una sola línea.\n`}
  tests={`assert correr().strip() == "Ruta: Python -> Java -> C++ -> JavaScript", f"imprimiste {correr().strip()!r}"\nassert len(correr().strip().splitlines()) == 1, "tiene que salir todo en UNA sola línea"\nassert correr(primero="Go", segundo="Rust", tercero="C", cuarto="Zig").strip() == "Ruta: Go -> Rust -> C -> Zig", "si cambian los lenguajes, la salida tiene que acompañar"`}
  pistas={[
    "Mirá los separadores: después de <code>Ruta:</code> hay un espacio, pero entre los lenguajes hay flechas. ¿Puede un mismo <code>print</code> usar dos separadores distintos a la vez?",
    "Si te sale en dos líneas: ¿qué agrega <code>print</code> al final de todo, y con qué parámetro se cambia?",
    "El segundo test cambia los cuatro lenguajes. ¿Qué pasaría si escribieras la ruta completa a mano dentro de las comillas?",
  ]}
>
Los cuatro lenguajes ya tienen valor. Mostrá la ruta en **una sola línea**:

```
Ruta: Python -> Java -> C++ -> JavaScript
```

Fijate que `Ruta:` va separado por un **espacio**, pero los lenguajes van separados por **flechas**.

Ojo: si cambian los lenguajes, tu programa tiene que seguir funcionando.
</EjercicioPython>

## Los tres errores que vas a cometer

| Error | Qué pasó |
|-------|----------|
| `NameError: name 'Hola' is not defined` | Te faltaron las comillas: Python creyó que era el nombre de algo. |
| `SyntaxError: unterminated string literal` | Abriste una comilla y no la cerraste. |
| `TypeError: can only concatenate str` | Usaste `+` entre texto y número. Probá con coma o f-string. |

:::tip[Leé el error de abajo para arriba]
Python te dice **el tipo de error** en la última línea y **en qué línea** de tu código pasó. Con esas
dos cosas ya sabés dónde mirar. No hace falta entender todo el bloque.
:::


## Más práctica

<EjercicioPython
  titulo="Nombre y apellido"
  dificultad="🌱"
  datos={`nombre = "Maxi"\napellido = "Nuñez"`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(nombre="Maxi", apellido="Nuñez").strip() == "Maxi Nuñez", f'imprimiste {correr(nombre="Maxi", apellido="Nuñez").strip()!r}'\nassert correr(nombre="Ada", apellido="Lovelace").strip() == "Ada Lovelace", "con otros datos tiene que acompañar"\nassert len(correr(nombre="Maxi", apellido="Nuñez").strip().splitlines()) == 1, "tiene que salir todo en UNA sola línea"`}
  pistas={[
    "Los dos valores tienen que salir en la misma línea, separados por un espacio. ¿Qué pasa si se los pasás a un mismo <code>print</code> separados por coma?",
  ]}
>
Mostrá el nombre y el apellido **en una sola línea**, separados por un espacio.
</EjercicioPython>

<EjercicioPython
  titulo="Separados por guion"
  dificultad="🌱"
  datos={`a = "Python"\nb = "es"\nc = "genial"`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr().strip() == "Python-es-genial", f"imprimiste {correr().strip()!r}"\nassert correr(a="uno", b="dos", c="tres").strip() == "uno-dos-tres", "con otras palabras tiene que acompañar"`}
  pistas={[
    "No queremos el espacio que <code>print</code> pone por defecto entre los valores. ¿Con qué parámetro se cambia ese separador?",
    "El separador va entre los valores, no al principio ni al final. Si te sale <code>-Python-es-genial</code>, revisá cómo lo estás armando.",
  ]}
>
Mostrá las tres palabras separadas por guiones, sin espacios:

```
Python-es-genial
```
</EjercicioPython>

<EjercicioPython
  titulo="Dos prints, una línea"
  dificultad="🌱"
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr()\nassert r.strip() == "Hola Mundo", f"imprimiste {r.strip()!r}"\nassert len(r.strip().splitlines()) == 1, "tiene que quedar todo en una sola línea"`}
  pistas={[
    "Cada <code>print</code> termina con un salto de línea. ¿Con qué parámetro se cambia lo que va al final?",
    "Si te queda <code>HolaMundo</code> todo pegado, ¿qué le falta a lo que pusiste como final del primer print?",
  ]}
>
Usando **dos `print()` separados**, hacé que en pantalla se lea:

```
Hola Mundo
```

La gracia es que quede en **una sola línea**, aunque sean dos instrucciones.
</EjercicioPython>

---

Siguiente: [Variables y tipos de datos](../variables/) — dónde guardar los valores para no repetirlos.
