---
title: Tuplas
description: "La colección que no se puede modificar: desempaquetado, devolver varios valores y zip."
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_2_funciones_y_colecciones/tuplas_sets/" />

## El problema

Guardemos las coordenadas de un punto en una lista:

```python
punto = [3, 5]
punto[0] = 99      # ...y ahora el punto está en otro lado
```

Nada te impide cambiarlo. Para una lista de compras eso está perfecto — querés agregar y sacar cosas.
Pero hay datos que **no deberían poder cambiar**: las coordenadas de un punto, una fecha, los colores
de una bandera, el nombre y el DNI de una persona.

Una **tupla** es una colección que, una vez creada, **no se puede modificar**.

```python
punto = (3, 5)
punto[0] = 99      # 💥 TypeError: 'tuple' object does not support item assignment
```

:::note[Que no se pueda cambiar es la ventaja]
Suena a limitación, pero es una garantía: si recibís una tupla, sabés que **nadie te la va a cambiar
por debajo**. Es el mismo espíritu que poner un dato en mayúsculas para decir "esto es una constante",
solo que acá Python lo hace cumplir de verdad.
:::

## Cómo se arma

```python
punto = (3, 5)
colores = ("rojo", "verde", "azul")
vacia = ()
un_solo = (42,)        # ← ojo con esa coma
```

Los paréntesis son opcionales; **lo que hace la tupla es la coma**:

```python
punto = 3, 5           # también es una tupla
```

:::caution[La tupla de un solo elemento]
```python
esto_no = (42)         # esto es el número 42, los paréntesis solo agrupan
esto_si = (42,)        # ESTO es una tupla de un elemento
```
Sin la coma no hay tupla. Es de esos detalles que te hacen perder veinte minutos la primera vez.
:::

Leer y recorrer funciona igual que en una lista:

```python
punto[0]                   # 3
len(colores)               # 3
"rojo" in colores          # True
for c in colores:
    print(c)
```

Lo único que no podés es **modificarla**: nada de `append`, `remove`, ni asignar por posición.

<EjercicioPython
  titulo="Los extremos"
  dificultad="🌱"
  datos={`numeros = [3, 9, 1, 7]`}
  starter={`# Mostrá una tupla con el menor y el mayor.\n`}
  tests={`import ast\nr = ast.literal_eval(correr(numeros=[3, 9, 1, 7]).strip())\nassert r == (1, 9), f"con [3, 9, 1, 7] imprimiste {r!r}, esperaba (1, 9)"\nassert isinstance(r, tuple), "tiene que ser una tupla, no una lista"\nassert ast.literal_eval(correr(numeros=[5]).strip()) == (5, 5), "con un solo número, el menor y el mayor son el mismo"`}
  pistas={[
    "Uno de los tests revisa que lo que mostrás sea una tupla y no una lista. ¿Qué las diferencia al escribirlas?",
    "¿Qué dos valores te está pidiendo, y en qué orden los espera el test?",
  ]}
>
Mostrá una **tupla** con el número más chico y el más grande de `numeros`, en ese orden.
</EjercicioPython>

## Desempaquetado: la parte que más se usa

Podés repartir una tupla en varias variables de una sola vez:

```python
punto = (3, 5)
x, y = punto           # x vale 3, y vale 5
```

Tiene que haber **tantas variables como elementos**, o Python se queja.

Esto explica un truco que quizás ya viste:

```python
a = 1
b = 2
a, b = b, a            # intercambia los valores, sin variable auxiliar
```

Del lado derecho se arma la tupla `(2, 1)` y del izquierdo se desempaqueta. Por eso funciona.

Y también explica el `for` sobre `.items()` de los diccionarios:

```python
for clave, valor in precios.items():
    ...
```

`.items()` va entregando tuplas de dos, y el `for` las desempaqueta en cada vuelta.

## Devolver más de un valor

:::note[Esto engancha con Funciones I]
Acá se ve para qué servía todo eso de `return`: una función puede devolver **varios** valores de una,
y lo que te llega es una tupla.
:::

Esta es **la** razón por la que vas a usar tuplas todo el tiempo. Una función devuelve una sola cosa…
pero si esa cosa es una tupla, en la práctica devuelve varias:

```python
def dividir(a, b):
    return a // b, a % b       # devuelve una tupla

cociente, resto = dividir(10, 3)     # y se desempaqueta al recibirla
print(cociente, resto)               # 3 1
```

En el ejercicio de repartir caramelos ya calculaste dos resultados de una sola vez (los que le
tocan a cada uno y los que sobran). Lo que faltaba era la forma de **devolver los dos juntos**.

<EjercicioPython
  titulo="Estadísticas de un tiro"
  dificultad="🌿"
  datos={`numeros = [2, 4, 6]`}
  starter={`# Mostrá una tupla: (cantidad, suma, promedio)\n`}
  tests={`import ast\nassert ast.literal_eval(correr(numeros=[2, 4, 6]).strip()) == (3, 12, 4.0), f"con [2, 4, 6] imprimiste {correr(numeros=[2, 4, 6]).strip()!r}"\nassert ast.literal_eval(correr(numeros=[5]).strip()) == (1, 5, 5.0), "un solo elemento"\nassert ast.literal_eval(correr(numeros=[]).strip()) == (0, 0, 0), "con la lista vacía mostrá (0, 0, 0) sin romper"`}
  pistas={[
    "Son tres valores en un orden fijo. Antes de escribir, nombrá cuáles son y en qué orden los pide el enunciado.",
    "El promedio necesita dividir por la cantidad. Mirá el test de la lista vacía: ¿por cuánto estarías dividiendo ahí?",
  ]}
>
Mostrá una tupla con **cuántos** números hay, su **suma** y su **promedio**, en ese orden.

Con la lista vacía, mostrá `(0, 0, 0)`.
</EjercicioPython>

## `zip`: recorrer dos listas a la vez

```python
nombres = ["Guada", "Brandon", "Yoda"]
notas = [10, 8, 9]

for nombre, nota in zip(nombres, notas):
    print(f"{nombre} sacó {nota}")
```

`zip` va emparejando: primero con primero, segundo con segundo. Devuelve tuplas, que el `for`
desempaqueta.

:::caution[`zip` corta con la más corta]
Si una lista tiene 3 elementos y la otra 5, `zip` te da 3 pares y **descarta los que sobran, sin
avisar**. Cuando las listas tendrían que ir apareadas, conviene chequear que midan lo mismo.
:::

<EjercicioPython
  titulo="Armar el boletín"
  dificultad="🌿"
  datos={`nombres = ["Ana", "Beto"]\nnotas = [10, 7]`}
  starter={`# Mostrá un diccionario que asocie cada nombre con su nota.\n`}
  tests={`import ast\nassert ast.literal_eval(correr(nombres=["Ana", "Beto"], notas=[10, 7]).strip()) == {"Ana": 10, "Beto": 7}, f'imprimiste {correr(nombres=["Ana", "Beto"], notas=[10, 7]).strip()!r}'\nassert ast.literal_eval(correr(nombres=[], notas=[]).strip()) == {}, "sin alumnos, diccionario vacío"\nassert ast.literal_eval(correr(nombres=["Ana"], notas=[9]).strip()) == {"Ana": 9}, "un solo alumno"`}
  pistas={[
    "Los dos datos de cada alumno están en listas distintas, en la misma posición. ¿Qué función viste que los va emparejando?",
    "El resultado no es una lista sino algo donde buscás por nombre. ¿Qué estructura es esa, y con qué valor arranca antes de cargar al primero?",
  ]}
>
Mostrá un **diccionario** que asocie cada nombre con su nota.

Con `["Ana", "Beto"]` y `[10, 7]` tiene que dar `{'Ana': 10, 'Beto': 7}`.
</EjercicioPython>

## ¿Lista o tupla?

| Usá **lista** cuando... | Usá **tupla** cuando... |
|---|---|
| la cantidad de elementos puede cambiar | son un conjunto fijo de datos |
| todos los elementos son lo mismo | cada posición significa algo distinto |
| necesitás agregar, sacar u ordenar | querés garantizar que nadie lo modifique |
| *notas de un alumno, productos del carrito* | *coordenadas, una fecha, un registro* |

:::tip[Un bonus concreto]
Las tuplas **pueden ser claves de un diccionario** y las listas no, justamente porque no cambian:

```python
distancias = {("Buenos Aires", "Córdoba"): 700}
```
:::


## 🏗️ El sistema: la marca que no se toca

Una marca de asistencia es **un DNI y un estado, juntos**: `("30111222", "P")`. Y es el ejemplo
perfecto de tupla, porque esos dos datos **no tienen sentido por separado** y porque, una vez
puesta, la marca no se edita en el lugar: se reemplaza entera.

La lista del día es entonces una **lista de tuplas**:

```python
marcas = [("30111222", "P"), ("28999888", "A"), ("27888444", "T")]
```

<EjercicioPython
  titulo="Una marca de asistencia"
  dificultad="🌱"
  starter={`def marcar(dni, estado):\n    # Devolvé la marca: el DNI y el estado, juntos.\n    pass`}
  tests={`m = marcar("30111222", "P")\nassert m == ("30111222", "P"), f"devolviste {m!r}"\nassert isinstance(m, tuple), "tiene que ser una TUPLA, no una lista: una marca no se edita, se reemplaza"\ntry:\n    m[1] = "A"\n    assert False, "si se pudo cambiar, no era una tupla"\nexcept TypeError:\n    pass\nassert marcar("1", "J") == ("1", "J")\nprint("¡Marca OK!")`}
  pistas={[
    "Los paréntesis son opcionales al crearla, pero conviene ponerlos: dejan claro que es una sola cosa y no dos.",
    "Uno de los tests intenta cambiarle un valor y espera que Python <em>no lo deje</em>. ¿Qué estructura no se puede modificar después de creada?",
  ]}
>
Completá `marcar(dni, estado)`: devuelve la marca de ese alumno ese día.

```python
marcar("30111222", "P")   # -> ("30111222", "P")
```

:::note[Por qué tupla y no lista]
Porque **una marca no se corrige a pedacitos**. Si preceptoría se equivocó, no cambia el estado
adentro de la marca: pone la marca de nuevo, entera. Usar una tupla no es un detalle de estilo —
es el programa diciendo *"esto viaja junto y no se edita"*, y Python lo hace cumplir.

Uno de los tests de acá arriba comprueba justamente eso: intenta cambiarla y espera que falle.
:::
</EjercicioPython>

<EjercicioPython
  titulo="Desempaquetar en el for"
  dificultad="🌿"
  datos={`marcas = [("30111222", "P"), ("28999888", "A"), ("27888444", "T"), ("26777333", "J")]`}
  starter={`def los_que_vinieron(marcas):\n    # Devolvé la lista de DNI de los que vinieron.\n    # La "T" cuenta como que vino.\n    pass`}
  tests={`r = los_que_vinieron(marcas)\nassert r == ["30111222", "27888444"], f"devolviste {r!r}"\nassert los_que_vinieron([]) == [], "sin marcas todavía, la lista vacía"\nassert los_que_vinieron([("1", "A"), ("2", "J")]) == [], "si no vino nadie, la lista vacía"\nassert los_que_vinieron([("9", "T")]) == ["9"], "el que llegó tarde vino"\nprint("¡Desempaquetado OK!")`}
  pistas={[
    "El <code>for</code> puede sacar las dos partes de la tupla de una: <code>for dni, estado in marcas:</code>",
    "Si escribís <code>for m in marcas</code> vas a tener que andar con <code>m[0]</code> y <code>m[1]</code>. Anda igual, pero dentro de tres meses nadie se acuerda cuál era cuál.",
  ]}
>
Completá `los_que_vinieron(marcas)`: devuelve **solo los DNI** de los que estuvieron en clase (la
`"T"` cuenta como que vino).

Es la lista que después usa la pantalla de la merienda.
</EjercicioPython>

<EjercicioPython
  titulo="El pie de la pantalla"
  dificultad="🌿"
  datos={`marcas = [("30111222", "P"), ("28999888", "A"), ("27888444", "T"), ("26777333", "J")]`}
  starter={`def resumen(marcas, total_del_curso):\n    # Devolvé (vinieron, faltaron, sin_marcar).\n    pass`}
  tests={`r = resumen(marcas, 10)\nassert r == (2, 2, 6), f"devolviste {r!r}"\nassert isinstance(r, tuple), "devolvé una tupla con los tres números"\nvinieron, faltaron, sin_marcar = resumen(marcas, 10)\nassert (vinieron, faltaron, sin_marcar) == (2, 2, 6), "los tres tienen que poder desempaquetarse"\nassert resumen([], 20) == (0, 0, 20), "un día sin empezar: nadie marcado, faltan los 20"\nassert resumen([("1", "P")], 1) == (1, 0, 0), "curso de uno, y vino"\nprint("¡Resumen OK!")`}
  pistas={[
    "Son tres cuentas sobre las mismas marcas: los que vinieron (P y T), los que no (A y J) y los que todavía nadie tocó.",
    "Los que faltan por marcar no están en la lista: son los que hay en el curso menos los que ya tienen marca.",
    "Para devolver los tres, ponelos separados por coma. Los paréntesis los pone Python solo.",
  ]}
>
Al pie de la pantalla de pasar lista van tres números: **vinieron · faltaron · sin marcar**.

Completá `resumen(marcas, total_del_curso)` para que los devuelva **los tres juntos**:

```python
resumen(marcas, 10)   # -> (2, 2, 6)
```

:::tip[Es la razón por la que existen las tuplas]
Una función devuelve **un** valor. Cuando hacen falta tres que van siempre juntos, la opción no es
escribir tres funciones que recorran la misma lista tres veces: es devolver una tupla.

Y del otro lado se desempaqueta en una línea:

```python
vinieron, faltaron, sin_marcar = resumen(marcas, 30)
```
:::
</EjercicioPython>

## Más práctica

<EjercicioPython
  titulo="Iteración con desempaquetado"
  dificultad="🌿"
  datos={`alumnos = [("Ana", 10), ("Beto", 7), ("Cami", 9)]`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr().strip().splitlines()[-3:]\nassert r == ["Ana sacó 10", "Beto sacó 7", "Cami sacó 9"], f"salió {r!r}"\nr2 = correr(alumnos=[("Tux", 5)]).strip().splitlines()[-1:]\nassert r2 == ["Tux sacó 5"], "con un solo alumno"`}
  pistas={[
    "Cada elemento de la lista es una tupla de dos. ¿Podés pedirle al <code>for</code> que te dé las dos partes por separado en cada vuelta?",
    "Si te sale algo como <code>('Ana', 10) sacó</code>, es que estás recibiendo la tupla entera en una sola variable.",
  ]}
>
`alumnos` es una lista de tuplas `(nombre, nota)`. Mostrá una línea por alumno:

```
Ana sacó 10
```
</EjercicioPython>

---

Siguiente: [Sets](../sets/) — la colección sin repetidos, ideal para preguntar "¿está o no está?".
