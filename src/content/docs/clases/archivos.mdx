---
title: Manejo de archivos
description: "Guardar datos que sobrevivan al programa: abrir, leer, escribir y por qué siempre se usa with."
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_3_persistencia/archivos/" />

## El problema

Todo lo que hiciste hasta ahora vive en la **memoria RAM**: existe mientras el programa corre y
desaparece cuando se cierra.

```python
alumnos = ["Guada", "Brandon", "Yoda"]
print(alumnos)
```

Cerrás el programa, lo volvés a abrir... y la lista no está más. Si tuvieras que cargar cien alumnos
a mano cada vez que arrancás, el programa sería inútil.

Para que los datos **sobrevivan**, hay que sacarlos de la RAM y ponerlos en el **disco**: un archivo.

:::note[La jerarquía en una frase]
La **RAM** es rápida pero se borra al apagar. El **disco** es más lento pero recuerda. Cuando querés
que algo dure entre ejecuciones, va al disco.
:::

## Abrir un archivo

```python
archivo = open("datos.txt", "w")
archivo.write("Hola")
archivo.close()
```

`open()` recibe **dónde** está el archivo y **para qué** lo abrís (el *modo*):

| Modo | Qué hace | Si el archivo no existe |
|---|---|---|
| `"r"` | Leer (es el que se usa por defecto) | 💥 Error |
| `"w"` | Escribir **borrando todo** lo anterior | Lo crea |
| `"a"` | Escribir **agregando al final** | Lo crea |
| `"x"` | Crear, y fallar si ya existe | Lo crea |

:::caution[`"w"` borra sin preguntar]
Abrir con `"w"` un archivo que ya tiene contenido lo **vacía en el acto**, antes incluso de que
escribas nada. Si querés sumar sin perder lo que había, el modo es `"a"`.
:::

## Siempre `with`

Ese `archivo.close()` del ejemplo es un problema esperando: si el programa falla entre el `open` y el
`close`, el archivo queda abierto y podés perder lo que escribiste.

Por eso en Python **siempre** se usa `with`:

```python
with open("datos.txt", "w", encoding="utf-8") as archivo:
    archivo.write("Hola")
# acá afuera ya está cerrado, incluso si algo falló adentro
```

El `with` se encarga de cerrarlo pase lo que pase. No es una preferencia de estilo: es la forma
correcta.

:::tip[¿Por qué `encoding="utf-8"`?]
Porque nuestros textos tienen tildes y ñ. Sin especificar el encoding, Python usa el del sistema
operativo, que cambia entre Windows y Linux — y el mismo programa que anda en tu casa te muestra
`Ã±` en la máquina del CFP. Ponelo siempre.
:::

## Leer

Tres formas, según lo que necesites:

```python
with open("datos.txt", "r", encoding="utf-8") as archivo:
    todo = archivo.read()          # un solo string con TODO

with open("datos.txt", "r", encoding="utf-8") as archivo:
    lineas = archivo.readlines()   # una lista, un elemento por línea

with open("datos.txt", "r", encoding="utf-8") as archivo:
    for linea in archivo:          # de a una, sin cargar todo en memoria
        print(linea, end="")
```

La tercera es la mejor para archivos grandes: no necesita meter el archivo entero en la RAM.

:::note[El `end=""` del `print`]
Cada línea que leés **ya trae su salto de línea** al final. Si además `print` agrega el suyo, te
quedan renglones vacíos en el medio. Por eso el `end=""`.
:::

<EjercicioPython
  titulo="Guardar una lista"
  dificultad="🌿"
  starter={`def guardar(ruta, items):\n    pass`}
  tests={`guardar("t1.txt", ["Guada", "Brandon", "Yoda"])\nwith open("t1.txt", encoding="utf-8") as f:\n    contenido = f.read()\nassert contenido == "Guada\\nBrandon\\nYoda\\n", f"el archivo quedó con {contenido!r}"\nguardar("t1.txt", ["Tux"])\nwith open("t1.txt", encoding="utf-8") as f:\n    assert f.read() == "Tux\\n", "al guardar de nuevo tiene que reemplazar, no agregar"\nprint("¡Archivo escrito!")`}
  pistas={[
    "Mirá el segundo tramo del test: guarda una lista nueva sobre el mismo archivo y espera que la anterior ya no esté. ¿Cuál de los modos hace eso, y cuál haría lo contrario?",
    "El test espera un salto de línea después del último nombre también. ¿<code>write</code> agrega ese salto por su cuenta o se lo tenés que poner vos?",
  ]}
>
Completá `guardar` para que escriba cada elemento de `items` en una línea del archivo `ruta`.

Si el archivo ya existía, el contenido viejo **se reemplaza**.
</EjercicioPython>

## Escribir: `write` y `print`

```python
archivo.write("Hola\n")           # NO agrega salto de línea: se lo ponés vos
print("Hola", file=archivo)       # sí lo agrega, como cualquier print
```

Las dos sirven. `print(..., file=archivo)` suele ser más cómoda justamente porque se encarga del
salto de línea.

<EjercicioPython
  titulo="Contar las líneas"
  dificultad="🌿"
  starter={`def contar_lineas(ruta):\n    pass`}
  tests={`with open("t2.txt", "w", encoding="utf-8") as f:\n    f.write("uno\\ndos\\ntres\\n")\nassert contar_lineas("t2.txt") == 3, f'esperaba 3 y devolviste {contar_lineas("t2.txt")!r}'\nwith open("t2.txt", "w", encoding="utf-8") as f:\n    f.write("")\nassert contar_lineas("t2.txt") == 0, "un archivo vacío tiene 0 líneas"\nprint("¡Contadas!")`}
  pistas={[
    "Si tuvieras el contenido como una lista donde cada elemento es una línea, ¿qué te faltaría hacer para saber cuántas son?",
    "Fijate en el test del archivo vacío. ¿Tu solución entra al bucle aunque sea una vez, o directamente no entra?",
  ]}
>
Completá `contar_lineas` para que devuelva **cuántas líneas** tiene el archivo.
</EjercicioPython>

<EjercicioPython
  titulo="Sumar sin borrar"
  dificultad="🌳"
  starter={`def agregar(ruta, linea):\n    pass`}
  tests={`with open("t3.txt", "w", encoding="utf-8") as f:\n    f.write("Guada\\n")\nagregar("t3.txt", "Brandon")\nagregar("t3.txt", "Yoda")\nwith open("t3.txt", encoding="utf-8") as f:\n    contenido = f.read()\nassert contenido == "Guada\\nBrandon\\nYoda\\n", f"el archivo quedó con {contenido!r}"\nprint("¡Agregados sin perder nada!")`}
  pistas={[
    "El test empieza con Guada ya escrito y espera que siga estando al final. ¿Qué le pasaría a Guada si abrieras el archivo en modo escritura común?",
    "Se llama a <code>agregar</code> dos veces seguidas y las dos tienen que quedar. ¿Desde qué posición del archivo empieza a escribir cada modo?",
  ]}
>
Completá `agregar` para que sume `linea` al final del archivo **sin borrar** lo que ya había.
</EjercicioPython>

## Rutas: dónde busca Python

Cuando escribís `open("datos.txt")`, Python lo busca **desde la carpeta donde estás parado**, no
desde donde está el archivo `.py`. Es la causa número uno del `FileNotFoundError`.

```python
open("datos.txt")                    # ruta relativa: al lado de donde ejecutás
open("clase_archivos/datos.txt")     # relativa, bajando una carpeta
open("/home/maxi/datos.txt")         # absoluta: siempre el mismo lugar
```

:::tip[Si te tira `FileNotFoundError`]
No es que el archivo no exista: es que **no está donde Python está mirando**. Antes de pelearte con
la ruta, preguntale dónde está parado:

```python
from pathlib import Path
print(Path.cwd())      # la carpeta actual
```
:::

## Trabajar con carpetas: `pathlib`

```python
from pathlib import Path

carpeta = Path("practica_archivos")

for archivo in carpeta.iterdir():    # recorre lo que hay adentro
    print(archivo.name)

Path("datos.txt").exists()           # ¿existe?
```

`Path` arma las rutas correctamente en Windows, Linux y macOS sin que tengas que pensar en las barras.

:::note[Los archivos de esta página son de mentira]
Python corre acá adentro del navegador, con un sistema de archivos **virtual**: los archivos que
creás existen de verdad para el programa, pero viven en la pestaña y desaparecen al recargar. No
toca nada de tu compu. En VS Code, el mismo código escribe archivos posta.
:::


## 🏗️ El sistema: que la lista del día sobreviva a cerrar el programa

Hasta ahora la lista del día vivía en un diccionario, y un diccionario se muere cuando el programa
termina. Estas tres funciones son las que hacen que **al día siguiente siga estando**.

<EjercicioPython
  titulo="Guardar la lista del día"
  dificultad="🌿"
  starter={`def guardar_dia(ruta, dia):\n    pass`}
  tests={`guardar_dia("dia1.txt", {"30111222": "P", "28999888": "A"})\nwith open("dia1.txt", encoding="utf-8") as f:\n    contenido = f.read()\nassert contenido == "28999888,A\\n30111222,P\\n", f"el archivo quedó con {contenido!r}"\nguardar_dia("dia1.txt", {"30111222": "T"})\nwith open("dia1.txt", encoding="utf-8") as f:\n    assert f.read() == "30111222,T\\n", "volver a guardar el día REEMPLAZA lo anterior, no lo agrega abajo"\nguardar_dia("dia1.txt", {})\nwith open("dia1.txt", encoding="utf-8") as f:\n    assert f.read() == "", "un día sin nadie marcado deja el archivo vacío, no rompe"\nprint("¡Día guardado!")`}
  pistas={[
    "Mirá el primer test: el DNI que en el diccionario está segundo sale primero en el archivo. Los renglones van ordenados por DNI.",
    "Un diccionario recuerda el orden en que se cargó, no el que vos querés. ¿Con qué recorrés sus claves ordenadas?",
    "El test espera un salto de línea después del último renglón también.",
  ]}
>
Completá `guardar_dia(ruta, dia)`: escribe un renglón por alumno, `dni,estado`, **ordenados por
DNI**.

```
28999888,A
30111222,P
```

Si el archivo ya existía, lo que había **se reemplaza**: la lista del día es una sola.

:::note[Por qué ordenados]
Para que el archivo se pueda **mirar**. Dos días seguidos ordenados igual se comparan de un
vistazo; dos días en el orden en que cada uno fue marcando, no. Es gratis al escribir y se paga
solo la primera vez que hay que revisar algo a mano.
:::
</EjercicioPython>

<EjercicioPython
  titulo="Leer la lista, y si no está arrancar vacío"
  dificultad="🌿"
  starter={`from pathlib import Path\n\ndef cargar_dia(ruta):\n    pass`}
  tests={`import os\nif os.path.exists("dia2.txt"):\n    os.remove("dia2.txt")\nassert cargar_dia("dia2.txt") == {}, "si el archivo no existe todavía, el día arranca vacío en vez de reventar"\nwith open("dia2.txt", "w", encoding="utf-8") as f:\n    f.write("30111222,P\\n28999888,T\\n")\nd = cargar_dia("dia2.txt")\nassert d == {"30111222": "P", "28999888": "T"}, f"volvió {d!r}"\nassert d["30111222"] == "P", "el estado no puede volver con el salto de línea pegado"\nwith open("dia2.txt", "w", encoding="utf-8") as f:\n    f.write("")\nassert cargar_dia("dia2.txt") == {}, "un archivo vacío es un día sin marcar, no un error"\nprint("¡Día cargado!")`}
  pistas={[
    "Cada renglón viene como <code>\"30111222,P\\n\"</code>. Hay que sacarle el salto de línea y partirlo por la coma.",
    "Para el archivo que no existe: podés preguntar antes si está, o intentar abrirlo y atajar el error. Las dos valen; la primera se lee mejor.",
    "Ojo con el archivo vacío: si tu código entra al bucle igual, fijate qué pasa cuando no hay ni un renglón.",
  ]}
>
Completá `cargar_dia(ruta)`: devuelve el diccionario `{dni: estado}` que estaba guardado.

**Si el archivo no existe, devolvé `{}`** — sin romper.

:::caution[Esta es la línea que decide si el sistema arranca]
El primer día no hay ningún archivo. Si `cargar_dia` revienta cuando el archivo falta, el sistema
no arranca nunca la primera vez — y eso es exactamente lo que pasa cuando lo instalás en la máquina
del CFP, delante de todos.

"Si no está, arrancá vacío" no es una excepción rara: es **el caso normal del primer día**.
:::
</EjercicioPython>

<EjercicioPython
  titulo="La copia de seguridad"
  dificultad="🌿"
  starter={`from pathlib import Path\n\ndef respaldar(ruta, fecha):\n    pass`}
  tests={`with open("asistencia.txt", "w", encoding="utf-8") as f:\n    f.write("30111222,P\\n")\nnombre = respaldar("asistencia.txt", "2026-09-16")\nassert nombre == "asistencia-2026-09-16.txt", f"esperaba 'asistencia-2026-09-16.txt' y devolviste {nombre!r}"\nwith open(nombre, encoding="utf-8") as f:\n    assert f.read() == "30111222,P\\n", "la copia tiene que tener lo mismo que el original"\nwith open("asistencia.txt", encoding="utf-8") as f:\n    assert f.read() == "30111222,P\\n", "el original no se toca"\nnombre2 = respaldar("asistencia.txt", "2026-09-18")\nassert nombre2 == "asistencia-2026-09-18.txt", "otra fecha, otro archivo: las copias no se pisan entre ellas"\nprint("¡Respaldo OK!")`}
  pistas={[
    "<code>Path(\"asistencia.txt\").stem</code> te da <code>\"asistencia\"</code> y <code>.suffix</code> te da <code>\".txt\"</code>. Con esas dos piezas armás el nombre nuevo.",
    "Copiar es leer todo de uno y escribirlo en el otro. No hace falta ninguna librería.",
    "Fijate que la función además <em>devuelve</em> el nombre que usó: quien la llama necesita saber dónde quedó la copia.",
  ]}
>
Completá `respaldar(ruta, fecha)`: copia el archivo a uno nuevo con la fecha en el nombre, y
**devuelve** cómo se llamó.

```python
respaldar("asistencia.txt", "2026-09-16")   # -> "asistencia-2026-09-16.txt"
```

El original queda intacto.

:::note[La copia que salva el año]
El sistema va a vivir en una netbook del CFP que se apaga de un tirón. Una copia por día, con la
fecha en el nombre, es la diferencia entre perder un día y perder el año.

Y por eso el nombre lleva la fecha y no algo como `asistencia-backup.txt`: un solo archivo de
respaldo se pisa a sí mismo, y el día que te das cuenta de que los datos estaban mal ya lo
respaldaste tres veces.
:::
</EjercicioPython>

## Más práctica

<EjercicioPython
  titulo="Contar palabras de un archivo"
  dificultad="🌿"
  starter={`def contar_palabras(ruta):\n    pass`}
  tests={`with open("p1.txt", "w", encoding="utf-8") as f:\n    f.write("hola mundo\\nchau mundo\\n")\nassert contar_palabras("p1.txt") == 4, f'esperaba 4 y devolviste {contar_palabras("p1.txt")!r}'\nwith open("p1.txt", "w", encoding="utf-8") as f:\n    f.write("una\\n")\nassert contar_palabras("p1.txt") == 1, "una sola palabra"\nwith open("p1.txt", "w", encoding="utf-8") as f:\n    f.write("")\nassert contar_palabras("p1.txt") == 0, "un archivo vacío tiene 0 palabras"\nprint("¡Contadas!")`}
  pistas={[
    "Las palabras están repartidas en varias líneas. ¿Te conviene leer línea por línea, o todo de una y después partir?",
    "¿Qué método parte un texto en palabras? ¿Qué usa como separador si no le decís nada?",
  ]}
>
Completá `contar_palabras` para que devuelva **cuántas palabras** tiene el archivo en total,
contando todas las líneas.
</EjercicioPython>

<EjercicioPython
  titulo="Log de ejecuciones"
  dificultad="🌳"
  starter={`def registrar(ruta, evento):\n    pass`}
  tests={`import os\nif os.path.exists("log.txt"):\n    os.remove("log.txt")\nregistrar("log.txt", "inicio")\nregistrar("log.txt", "fin")\nwith open("log.txt", encoding="utf-8") as f:\n    lineas = f.read().strip().splitlines()\nassert lineas == ["1. inicio", "2. fin"], f"el log quedó con {lineas!r}"\nregistrar("log.txt", "otra")\nwith open("log.txt", encoding="utf-8") as f:\n    assert f.read().strip().splitlines()[-1] == "3. otra", "el número tiene que seguir la cuenta"\nprint("¡Log OK!")`}
  pistas={[
    "Cada evento se numera según cuántos había antes. ¿Cómo te enterás de eso si el archivo ya existe de una corrida anterior?",
    "La primera vez el archivo todavía no existe. ¿Qué pasa si intentás leerlo en modo lectura? ¿Con qué número arrancás en ese caso?",
  ]}
>
Completá `registrar` para que agregue una línea numerada al final del archivo, **sin borrar** lo
anterior:

```
1. inicio
2. fin
3. otra
```
</EjercicioPython>

---

Siguiente: JSON — guardar estructuras enteras (listas, diccionarios) y no solamente texto plano.
