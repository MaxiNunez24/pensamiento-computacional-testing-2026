---
title: Listas — guardar y recorrer muchos datos
description: La estructura que más vas a usar. Crear, recorrer y transformar listas, practicando con ejercicios que se verifican solos.
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_1_fundamentos/listas/" />


:::tip[Antes de empezar]
Clase **interactiva**: escribí, tocá **▶ Ejecutar** para probar y **✓ Verificar** (o `Ctrl/Cmd+Enter`)
para que los tests te digan si está bien.
:::

## El problema

Querés guardar las notas de un curso. Con variables sueltas sería:

```python
nota1 = 8
nota2 = 10
nota3 = 6
# ...¿y si son 30 alumnos? ¿nota30?
```

Imposible de manejar. Necesitás **una sola variable que guarde muchos valores** y los puedas recorrer
de a uno. Para eso están las **listas**.

## El concepto

Una lista es una **colección ordenada** de valores, entre corchetes y separados por coma:

```python
notas = [8, 10, 6, 9]

print(notas[0])      # 8   (el primero: la posición arranca en 0)
print(len(notas))    # 4   (cuántos elementos tiene)
notas.append(7)      # agrega 7 al final  -> [8, 10, 6, 9, 7]
```

Dos cosas que vas a usar todo el tiempo:

- **Índice**: `notas[0]` es el primero, `notas[-1]` el último.
- **`len(lista)`**: la cantidad de elementos.

:::note[¿Y para recorrerla entera?]
Seguro se te ocurrió: "¿tengo que escribir `notas[0]`, `notas[1]`, `notas[2]`… una por una?". No — se
recorre con un `for`, y es **justo la clase que sigue**. Por ahora quedate con cómo se arma una lista
y cómo se llega a un elemento; recorrerla viene enseguida.
:::

:::caution[La posición arranca en 0]
El primer elemento es `lista[0]`, no `lista[1]`. Si una lista tiene 4 elementos, el último es
`lista[3]` (o `lista[-1]`).
:::

## Manos a la obra

:::note[Sobre estos ejercicios]
Acá vas a escribir **código directo** (sin funciones todavía — eso viene más adelante). Partís de una
lista que ya está dada. El verificador mira lo que imprimís o la variable que creás. 😉
:::

<EjercicioPython
  titulo="Primero, último y cuántos"
  dificultad="🌱"
  datos={`equipos = ["Argentina", "Francia", "México", "Japón"]`}
  starter={`# 'equipos' ya tiene valor. Mostrá tres líneas:\n# el primero, el último y cuántos son.\n`}
  tests={`r = correr().strip().splitlines()[-3:]\nassert r == ["Argentina", "Japón", "4"], f"salió {r!r}"\nr2 = correr(equipos=["Uruguay", "Brasil"]).strip().splitlines()[-3:]\nassert r2 == ["Uruguay", "Brasil", "2"], "con otra lista tiene que acompañar"\nr3 = correr(equipos=["Chile"]).strip().splitlines()[-3:]\nassert r3 == ["Chile", "Chile", "1"], "con uno solo, el primero y el último son el mismo"`}
  pistas={[
    "El segundo test cambia la lista entera. ¿Qué pasa si escribís <code>Japón</code> a mano en vez de sacarlo de la lista?",
    "Para el último: podés contar cuántos hay y restar uno, o pedirlo desde el final. ¿Cuál de las dos no se rompe si la lista cambia de tamaño?",
  ]}
>
Mostrá, en tres líneas: el **primer** equipo, el **último** y **cuántos** hay.

Con la lista de arriba tiene que salir:

```
Argentina
Japón
4
```
</EjercicioPython>

<EjercicioPython
  titulo="Sumar un equipo"
  dificultad="🌱"
  datos={`equipos = ["Argentina", "Francia"]`}
  starter={`# Agregá "Brasil" al final de 'equipos' y después mostrá la lista.\n`}
  tests={`r = correr().strip().splitlines()[-1]\nassert r == "['Argentina', 'Francia', 'Brasil']", f"salió {r!r}"\nr2 = correr(equipos=[]).strip().splitlines()[-1]\nassert r2 == "['Brasil']", "si la lista viene vacía, queda solo Brasil"`}
  pistas={[
    "Buscás el método que <em>agrega al final</em>. ¿Cómo se dice \"agregar al final\" en inglés?",
    "Ojo: el método modifica la lista en el lugar, no devuelve una lista nueva. ¿Qué pasa si hacés <code>equipos = equipos.append(...)</code>?",
  ]}
>
Agregá `"Brasil"` al final de `equipos` y después mostrá la lista completa.

Tiene que salir: `['Argentina', 'Francia', 'Brasil']`
</EjercicioPython>

## Para llevar

- Lista: `[a, b, c]` — colección ordenada, índice desde `0`.
- `lista[i]`, `lista[-1]`, `len(lista)`, `lista.append(x)`.
- `sorted(lista)` devuelve una **nueva** ordenada; `lista.sort()` **modifica** la original.
- Para recorrerla entera se usa `for`, que es la clase que sigue.

## Más práctica

<EjercicioPython
  titulo="Ordenar sin modificar"
  dificultad="🌿"
  datos={`numeros = [5, 3, 8, 1]`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr().strip().splitlines()[-2:]\nassert r == ["[1, 3, 5, 8]", "[5, 3, 8, 1]"], f"salió {r!r}"\nr2 = correr(numeros=[2, 1]).strip().splitlines()[-2:]\nassert r2 == ["[1, 2]", "[2, 1]"], "con otra lista tiene que acompañar"`}
  pistas={[
    "Hay dos formas de ordenar: una cambia la lista original y la otra devuelve una nueva. ¿Cuál necesitás acá?",
    "Si la segunda línea te sale ordenada, es que usaste la que modifica. ¿Cuál es la otra?",
  ]}
>
Mostrá primero la lista **ordenada** y después la **original sin tocar**:

```
[1, 3, 5, 8]
[5, 3, 8, 1]
```
</EjercicioPython>

:::note[📦 Tres funciones que ya vienen con Python]
No hace falta recorrer la lista para sacar estas tres: Python ya las trae.

| Función | Qué devuelve | Ejemplo |
|---------|--------------|---------|
| `min(lista)` | el valor más chico | `min([4, 8, 6])` → `4` |
| `max(lista)` | el valor más grande | `max([4, 8, 6])` → `8` |
| `sum(lista)` | la suma de todos | `sum([4, 8, 6])` → `18` |
:::

<EjercicioPython
  titulo="La nota más alta, la más baja y el total"
  dificultad="🌿"
  datos={`notas = [7, 10, 4, 9]`}
  starter={`# Mostrá tres líneas: la más baja, la más alta y la suma.\n`}
  tests={`r = correr().strip().splitlines()[-3:]\nassert r == ["4", "10", "30"], f"salió {r!r}"\nr2 = correr(notas=[5, 5]).strip().splitlines()[-3:]\nassert r2 == ["5", "5", "10"], "si son todas iguales, la más baja y la más alta son la misma"\nr3 = correr(notas=[8]).strip().splitlines()[-3:]\nassert r3 == ["8", "8", "8"], "con una sola nota"`}
  pistas={[
    "Las tres salen de funciones que Python ya trae, sin recorrer nada. ¿Cuáles son?",
    "Fijate el orden que piden los tests: primero la más baja. ¿Coincide con el orden en que las escribiste?",
  ]}
>
Mostrá, en tres líneas: la nota **más baja**, la **más alta** y el **total** (la suma).

Con `[7, 10, 4, 9]` tiene que salir:

```
4
10
30
```
</EjercicioPython>

:::note[¿Qué sigue?]
En la próxima clase, **bucles**: recorrer la lista entera para transformarla, filtrarla o contar lo
que cumple una condición. Y más adelante, *list comprehensions* (hacer todo eso en una sola línea) y
listas de listas (matrices).
:::
