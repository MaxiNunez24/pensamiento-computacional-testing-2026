---
title: Listas — guardar y recorrer muchos datos
description: La estructura que más vas a usar. Crear, recorrer y transformar listas, practicando con ejercicios que se verifican solos.
---

import EjercicioPython from '../../../components/EjercicioPython.astro';

:::tip[Antes de empezar]
Clase **interactiva**: escribí, tocá **▶ Ejecutar** para probar y **✓ Verificar** (o `Ctrl/Cmd+Enter`)
para que los tests te digan si está bien.
:::

## El problema

Querés guardar las notas de un curso. Con variables sueltas sería:

```python
nota1 = 8
nota2 = 10
nota3 = 6
# ...¿y si son 30 alumnos? ¿nota30?
```

Imposible de manejar. Necesitás **una sola variable que guarde muchos valores** y los puedas recorrer
de a uno. Para eso están las **listas**.

## El concepto

Una lista es una **colección ordenada** de valores, entre corchetes y separados por coma:

```python
notas = [8, 10, 6, 9]

print(notas[0])      # 8   (el primero: la posición arranca en 0)
print(len(notas))    # 4   (cuántos elementos tiene)
notas.append(7)      # agrega 7 al final  -> [8, 10, 6, 9, 7]

for n in notas:      # recorrer de a uno
    print(n)
```

Tres cosas que vas a usar todo el tiempo:

- **Índice**: `notas[0]` es el primero, `notas[-1]` el último.
- **`len(lista)`**: la cantidad de elementos.
- **`for x in lista:`**: recorrerla elemento por elemento.

:::caution[La posición arranca en 0]
El primer elemento es `lista[0]`, no `lista[1]`. Si una lista tiene 4 elementos, el último es
`lista[3]` (o `lista[-1]`).
:::

## Manos a la obra

<EjercicioPython
  titulo="Transformar una lista"
  dificultad="🌱"
  starter={`def duplicar(numeros):\n    # Devolvé una lista NUEVA con cada número multiplicado por 2\n    pass`}
  tests={`assert duplicar([1, 2, 3]) == [2, 4, 6], "duplicar([1,2,3]) debería dar [2,4,6]"\nassert duplicar([]) == [], "una lista vacía debería devolver una lista vacía"\nassert duplicar([0, -4]) == [0, -8]\nprint("Listas duplicadas OK")`}
  pistas={[
    "Empezá con una lista vacía: <code>resultado = []</code>.",
    "Recorré con <code>for n in numeros:</code> y usá <code>resultado.append(...)</code>.",
  ]}
  solucion={`def duplicar(numeros):\n    resultado = []\n    for n in numeros:\n        resultado.append(n * 2)\n    return resultado`}
>
Escribí `duplicar(numeros)` que reciba una lista de números y **devuelva una lista nueva** con cada
número multiplicado por 2.

Ejemplo: `duplicar([1, 2, 3])` → `[2, 4, 6]`.
</EjercicioPython>

<EjercicioPython
  titulo="Filtrar elementos"
  dificultad="🌿"
  starter={`def solo_pares(numeros):\n    # Devolvé una lista solo con los números pares\n    pass`}
  tests={`assert solo_pares([1, 2, 3, 4]) == [2, 4]\nassert solo_pares([1, 3, 5]) == []\nassert solo_pares([10, 7, 8]) == [10, 8]\nprint("Pares filtrados OK")`}
  pistas={[
    "Un número es par si el resto de dividirlo por 2 es 0: <code>n % 2 == 0</code>.",
    "Recorré la lista y agregá solo los que cumplen la condición.",
  ]}
  solucion={`def solo_pares(numeros):\n    resultado = []\n    for n in numeros:\n        if n % 2 == 0:\n            resultado.append(n)\n    return resultado`}
>
Escribí `solo_pares(numeros)` que **devuelva** una lista nueva con **solo los números pares** de la
lista original (en el mismo orden).

Ejemplo: `solo_pares([1, 2, 3, 4])` → `[2, 4]`.
</EjercicioPython>

<EjercicioPython
  titulo="Contar con una condición"
  dificultad="🌿"
  starter={`def contar_mayores(numeros, limite):\n    # ¿Cuántos números son ESTRICTAMENTE mayores que 'limite'?\n    pass`}
  tests={`assert contar_mayores([1, 5, 8, 3], 4) == 2\nassert contar_mayores([1, 2, 3], 10) == 0\nassert contar_mayores([7, 7, 7], 6) == 3\nprint("Conteos OK")`}
  pistas={[
    "Llevá un contador en una variable que arranque en 0.",
    "Por cada número mayor que <code>limite</code>, sumale 1 al contador.",
  ]}
  solucion={`def contar_mayores(numeros, limite):\n    cantidad = 0\n    for n in numeros:\n        if n > limite:\n            cantidad += 1\n    return cantidad`}
>
Escribí `contar_mayores(numeros, limite)` que **devuelva cuántos** números de la lista son
**estrictamente mayores** que `limite`.

Ejemplo: `contar_mayores([1, 5, 8, 3], 4)` → `2`.
</EjercicioPython>

## Para llevar

- Lista: `[a, b, c]` — colección ordenada, índice desde `0`.
- `lista[i]`, `lista[-1]`, `len(lista)`, `lista.append(x)`.
- Patrón estrella: **lista vacía → recorrer con `for` → `append` si cumple → `return`**.

:::note[¿Qué sigue?]
Más adelante: *list comprehensions* (hacer lo de arriba en una sola línea) y listas de listas
(matrices).
:::
