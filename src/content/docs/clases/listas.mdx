---
title: Listas — guardar y recorrer muchos datos
description: La estructura que más vas a usar. Crear, recorrer y transformar listas, practicando con ejercicios que se verifican solos.
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/python/04_listas/listas/" />


:::tip[Antes de empezar]
Clase **interactiva**: escribí, tocá **▶ Ejecutar** para probar y **✓ Verificar** (o `Ctrl/Cmd+Enter`)
para que los tests te digan si está bien.
:::

## El problema

Querés guardar las notas de un curso. Con variables sueltas sería:

```python
nota1 = 8
nota2 = 10
nota3 = 6
# ...¿y si son 30 alumnos? ¿nota30?
```

Imposible de manejar. Necesitás **una sola variable que guarde muchos valores** y los puedas recorrer
de a uno. Para eso están las **listas**.

## El concepto

Una lista es una **colección ordenada** de valores, entre corchetes y separados por coma:

```python
notas = [8, 10, 6, 9]

print(notas[0])      # 8   (el primero: la posición arranca en 0)
print(len(notas))    # 4   (cuántos elementos tiene)
notas.append(7)      # agrega 7 al final  -> [8, 10, 6, 9, 7]

for n in notas:      # recorrer de a uno
    print(n)
```

Tres cosas que vas a usar todo el tiempo:

- **Índice**: `notas[0]` es el primero, `notas[-1]` el último.
- **`len(lista)`**: la cantidad de elementos.
- **`for x in lista:`**: recorrerla elemento por elemento.

:::caution[La posición arranca en 0]
El primer elemento es `lista[0]`, no `lista[1]`. Si una lista tiene 4 elementos, el último es
`lista[3]` (o `lista[-1]`).
:::

## Manos a la obra

:::note[Sobre estos ejercicios]
Acá vas a escribir **código directo** (sin funciones todavía — eso viene más adelante). Partís de una
lista que ya está dada y tenés que **crear una variable nueva** con el resultado. El verificador
revisa esa variable. 😉
:::

<EjercicioPython
  titulo="Transformar una lista"
  dificultad="🌱"
  starter={`numeros = [1, 2, 3]\n\n# Creá una lista 'dobles' con cada número multiplicado por 2\n`}
  tests={`assert dobles == [2, 4, 6], "dobles debería ser [2, 4, 6]"\nprint("¡Lista duplicada!")`}
  pistas={[
    "Si tuvieras que hacerlo a mano con lápiz: ¿arrancás con una hoja en blanco o ya escribiste algo? ¿Qué representa esa hoja en tu código?",
    "Por cada número de la lista original vas a producir uno nuevo. ¿Dónde lo vas guardando para no perderlo?",
  ]}
>
Tenés la lista `numeros = [1, 2, 3]`. Creá una lista llamada **`dobles`** con cada número
multiplicado por 2.

Resultado esperado: `dobles` debería ser `[2, 4, 6]`.
</EjercicioPython>

<EjercicioPython
  titulo="Filtrar elementos"
  dificultad="🌿"
  starter={`numeros = [1, 2, 3, 4, 5, 6]\n\n# Creá una lista 'pares' con solo los números pares de 'numeros'\n`}
  tests={`assert pares == [2, 4, 6], "pares debería ser [2, 4, 6]"\nprint("¡Pares filtrados!")`}
  pistas={[
    "¿Cómo te das cuenta de que un número es par sin usar la calculadora? ¿Qué te tiene que dar la división por 2?",
    "Esta vez no todos los elementos pasan a la lista nueva. ¿En qué momento del recorrido decidís si este entra o no?",
  ]}
>
Tenés la lista `numeros = [1, 2, 3, 4, 5, 6]`. Creá una lista **`pares`** con **solo los números
pares** (en el mismo orden).

Resultado esperado: `pares` debería ser `[2, 4, 6]`.
</EjercicioPython>

<EjercicioPython
  titulo="Contar con una condición"
  dificultad="🌿"
  starter={`numeros = [1, 5, 8, 3, 9, 2]\nlimite = 4\n\n# Guardá en 'cantidad' cuántos números son MAYORES que 'limite'\n`}
  tests={`assert cantidad == 3, "cantidad debería ser 3 (5, 8 y 9)"\nprint("¡Conteo correcto!")`}
  pistas={[
    "Si contaras a mano con el dedo, ¿en qué número arrancás antes de mirar el primer elemento?",
    "¿Qué querés que quede al final: la lista de los que cumplen, o solamente cuántos son?",
  ]}
>
Tenés `numeros = [1, 5, 8, 3, 9, 2]` y `limite = 4`. Guardá en **`cantidad`** cuántos números son
**estrictamente mayores** que `limite`.

Resultado esperado: `cantidad` debería ser `3` (son el 5, el 8 y el 9).
</EjercicioPython>

## Para llevar

- Lista: `[a, b, c]` — colección ordenada, índice desde `0`.
- `lista[i]`, `lista[-1]`, `len(lista)`, `lista.append(x)`.
- Patrón estrella: **lista vacía → recorrer con `for` → `append` si cumple → `return`**.

## Más práctica

<EjercicioPython
  titulo="Ordenar sin modificar"
  dificultad="🌿"
  datos={`numeros = [5, 3, 8, 1]`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr().strip().splitlines()[-2:]\nassert r == ["[1, 3, 5, 8]", "[5, 3, 8, 1]"], f"salió {r!r}"\nr2 = correr(numeros=[2, 1]).strip().splitlines()[-2:]\nassert r2 == ["[1, 2]", "[2, 1]"], "con otra lista tiene que acompañar"`}
  pistas={[
    "Hay dos formas de ordenar: una cambia la lista original y la otra devuelve una nueva. ¿Cuál necesitás acá?",
    "Si la segunda línea te sale ordenada, es que usaste la que modifica. ¿Cuál es la otra?",
  ]}
>
Mostrá primero la lista **ordenada** y después la **original sin tocar**:

```
[1, 3, 5, 8]
[5, 3, 8, 1]
```
</EjercicioPython>

<EjercicioPython
  titulo="Mayores al promedio"
  dificultad="🌳"
  datos={`notas = [4, 8, 6, 10]`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`import ast\nassert ast.literal_eval(correr().strip().splitlines()[-1]) == [8, 10], f"salió {correr().strip().splitlines()[-1]!r}"\nassert ast.literal_eval(correr(notas=[5, 5, 5]).strip().splitlines()[-1]) == [], "si son todas iguales, ninguna supera el promedio"\nassert ast.literal_eval(correr(notas=[1, 9]).strip().splitlines()[-1]) == [9], "solo el 9 supera el promedio de 5"`}
  pistas={[
    "Necesitás el promedio ANTES de poder comparar. ¿Se puede calcular mientras recorrés, o hay que sacarlo primero?",
    "Fijate en el test donde todas las notas son iguales: ninguna tiene que aparecer. ¿Estás usando mayor, o mayor o igual?",
  ]}
>
Mostrá una lista con las notas que están **por encima del promedio**.

Con `[4, 8, 6, 10]` (promedio 7) tiene que dar `[8, 10]`.
</EjercicioPython>

:::note[¿Qué sigue?]
Más adelante: *list comprehensions* (hacer lo de arriba en una sola línea) y listas de listas
(matrices).
:::
