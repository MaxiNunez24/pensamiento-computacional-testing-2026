---
title: Condicionales — if / elif / else
description: "Que el programa decida: comparaciones, ramas y el orden en que se evalúan."
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/python/03_estructuras_de_control/if_else/" />

## El problema

Hasta ahora tus programas hacían siempre lo mismo, en el mismo orden. Pero la mayoría de las cosas
útiles necesitan **decidir**:

- Si la nota es 6 o más → aprobó. Si no → desaprobó.
- Si el carrito supera los $50.000 → envío gratis.
- Si la contraseña coincide → entra.

Para eso está el `if`.

## La forma básica

```python
edad = 20

if edad >= 18:
    print("Puede votar")
else:
    print("Todavía no")
```

Tres cosas que importan:

- Después de la **condición** van **dos puntos** `:`.
- Lo que está **indentado** (corrido a la derecha) es lo que pasa si la condición se cumple.
- El `else` es **opcional**: si no hay nada que hacer cuando no se cumple, no lo pongas.

:::caution[La indentación no es decoración]
En Python, los espacios de la izquierda **son parte del código**. Le dicen qué líneas están adentro
del `if` y cuáles afuera.

```python
if edad >= 18:
    print("Puede votar")     # adentro: solo si es mayor
print("Fin del programa")    # afuera: siempre
```
Si movés esa última línea cuatro espacios a la derecha, cambia lo que hace el programa.
:::

## Comparaciones

| Operador | Significa |
|---|---|
| `==` | ¿son iguales? |
| `!=` | ¿son distintos? |
| `>` `<` | mayor / menor |
| `>=` `<=` | mayor o igual / menor o igual |

:::caution[`=` y `==` no son lo mismo]
`=` **guarda** un valor (`edad = 18`). `==` **pregunta** si dos cosas son iguales (`edad == 18`).
Confundirlos es un clásico, y Python te lo marca como error de sintaxis.
:::

<EjercicioPython
  titulo="¿Aprobó?"
  dificultad="🌱"
  datos={`nota = 7`}
  starter={`# 'nota' ya tiene valor. Mostrá 'aprobado' o 'desaprobado'.\n`}
  tests={`assert correr(nota=7).strip() == "aprobado", f'con 7 imprimiste {correr(nota=7).strip()!r}'\nassert correr(nota=6).strip() == "aprobado", "6 es el mínimo para aprobar"\nassert correr(nota=5).strip() == "desaprobado", "con 5 no aprueba"\nassert correr(nota=10).strip() == "aprobado", "con 10 aprueba"`}
  pistas={[
    "Fijate en el test del 6. ¿El 6 tiene que aprobar o no? ¿Qué operador de comparación incluye al número que estás comparando y cuál lo deja afuera?",
  ]}
>
Mostrá `aprobado` si la nota es **6 o más**, y `desaprobado` si no.
</EjercicioPython>

## Más de dos caminos: `elif`

Cuando hay varias opciones, se encadenan con `elif` (*else if*):

```python
if nota >= 9:
    print("Excelente")
elif nota >= 7:
    print("Muy bien")
elif nota >= 6:
    print("Aprobado")
else:
    print("Desaprobado")
```

**El orden importa muchísimo.** Python prueba las condiciones de arriba hacia abajo y se queda con
**la primera que se cumple**; el resto ni las mira.

:::note[Por qué este orden y no otro]
Si pusieras `nota >= 6` primero, un 10 entraría por ahí y diría "Aprobado", porque 10 también es
mayor o igual a 6. Nunca llegaría a "Excelente". Con `elif` conviene ir **de lo más exigente a lo
menos exigente**.
:::

<EjercicioPython
  titulo="La categoría de la nota"
  dificultad="🌿"
  datos={`nota = 8`}
  starter={`# Mostrá la categoría que le corresponde a 'nota'.\n`}
  tests={`assert correr(nota=10).strip() == "Excelente", f'con 10 imprimiste {correr(nota=10).strip()!r}'\nassert correr(nota=9).strip() == "Excelente", "9 ya es Excelente"\nassert correr(nota=8).strip() == "Muy bien", f'con 8 imprimiste {correr(nota=8).strip()!r}'\nassert correr(nota=7).strip() == "Muy bien", "7 ya es Muy bien"\nassert correr(nota=6).strip() == "Aprobado", f'con 6 imprimiste {correr(nota=6).strip()!r}'\nassert correr(nota=5).strip() == "Desaprobado", "con 5 ya no alcanza"\nassert correr(nota=1).strip() == "Desaprobado", "con 1 tampoco"`}
  pistas={[
    "Probá tu solución con un 10 y mirá qué imprime. Si te dice “Aprobado”, ¿por cuál de las condiciones entró primero?",
    "Python se queda con la PRIMERA condición que se cumple. ¿En qué orden conviene preguntarlas para que un 10 no entre por la puerta del 6?",
  ]}
>
Mostrá la categoría de `nota`:

- `Excelente` con 9 o 10
- `Muy bien` con 7 u 8
- `Aprobado` con 6
- `Desaprobado` con menos de 6
</EjercicioPython>

## Combinar condiciones: `and`, `or`, `not`

```python
if edad >= 18 and tiene_dni:      # las DOS cosas
if es_finde or es_feriado:        # al menos UNA
if not esta_lleno:                # al revés
```

| | Da `True` cuando... |
|---|---|
| `A and B` | **las dos** son verdaderas |
| `A or B` | **al menos una** es verdadera |
| `not A` | A es falsa |

<EjercicioPython
  titulo="¿Puede entrar?"
  dificultad="🌿"
  datos={`edad = 20\ntiene_entrada = True`}
  starter={`# Mostrá 'pasá' o 'no pasás'.\n`}
  tests={`assert correr(edad=20, tiene_entrada=True).strip() == "pasá", "mayor y con entrada"\nassert correr(edad=20, tiene_entrada=False).strip() == "no pasás", "sin entrada no entra, aunque sea mayor"\nassert correr(edad=15, tiene_entrada=True).strip() == "no pasás", "menor no entra, aunque tenga entrada"\nassert correr(edad=18, tiene_entrada=True).strip() == "pasá", "18 ya es mayor de edad"\nassert correr(edad=15, tiene_entrada=False).strip() == "no pasás", "ni una ni la otra"`}
  pistas={[
    "Mirá los cinco casos de los tests. ¿En cuántos de ellos entra? ¿Qué tienen en común solo esos?",
    "¿Alcanza con que se cumpla una de las dos condiciones, o hacen falta las dos a la vez?",
  ]}
>
Para entrar al recital hay que ser **mayor de edad** (18 o más) **y** tener entrada.

Mostrá `pasá` o `no pasás`.
</EjercicioPython>

## Más práctica

<EjercicioPython
  titulo="Mayor de edad"
  dificultad="🌱"
  datos={`edad = 20`}
  starter={`# Mostrá 'mayor' o 'menor'.\n`}
  tests={`assert correr(edad=20).strip() == "mayor", "20 es mayor de edad"\nassert correr(edad=18).strip() == "mayor", "18 ya cuenta"\nassert correr(edad=17).strip() == "menor", "17 todavía no"\nassert correr(edad=0).strip() == "menor", "recién nacido"`}
  pistas={[
    "¿A partir de qué edad exactamente? ¿Ese número entra o queda afuera?",
  ]}
>
Mostrá `mayor` si la edad es 18 o más, y `menor` si no.
</EjercicioPython>

<EjercicioPython
  titulo="Positivo, negativo o cero"
  dificultad="🌱"
  datos={`numero = 5`}
  starter={`# Mostrá 'positivo', 'negativo' o 'cero'.\n`}
  tests={`assert correr(numero=5).strip() == "positivo", f'con 5 imprimiste {correr(numero=5).strip()!r}'\nassert correr(numero=-5).strip() == "negativo", f'con -5 imprimiste {correr(numero=-5).strip()!r}'\nassert correr(numero=0).strip() == "cero", "el 0 no es ni positivo ni negativo"\nassert correr(numero=0.1).strip() == "positivo", "un decimal chiquito sigue siendo positivo"`}
  pistas={[
    "Son tres resultados posibles, no dos. ¿El cero entra en alguno de los otros dos casos?",
  ]}
>
Mostrá `positivo`, `negativo` o `cero` según lo que valga `numero`.
</EjercicioPython>

<EjercicioPython
  titulo="El mayor de dos"
  dificultad="🌱"
  datos={`a = 8\nb = 3`}
  starter={`# Mostrá el mayor de los dos.\n`}
  tests={`assert correr(a=8, b=3).strip() == "8", "8 es mayor que 3"\nassert correr(a=3, b=8).strip() == "8", "el orden no debería importar"\nassert correr(a=5, b=5).strip() == "5", "si son iguales, mostrá ese mismo número"\nassert correr(a=-2, b=-9).strip() == "-2", "con negativos también"`}
  pistas={[
    "Fijate en el test donde los dos son iguales: ¿tu programa llega a imprimir algo en ese caso, o se queda sin mostrar nada?",
  ]}
>
Mostrá el mayor entre `a` y `b`. Si son iguales, mostrá ese número.
</EjercicioPython>

<EjercicioPython
  titulo="Par o impar"
  dificultad="🌱"
  datos={`numero = 4`}
  starter={`# Mostrá 'par' o 'impar'.\n`}
  tests={`assert correr(numero=4).strip() == "par", "4 es par"\nassert correr(numero=7).strip() == "impar", "7 es impar"\nassert correr(numero=0).strip() == "par", "el 0 es par"\nassert correr(numero=-3).strip() == "impar", "los negativos también son pares o impares"`}
  pistas={[
    "¿Cómo te das cuenta a ojo de que un número es par? ¿Qué tiene que dar al dividirlo por 2?",
  ]}
>
Mostrá `par` o `impar` según lo que valga `numero`.
</EjercicioPython>

<EjercicioPython
  titulo="Frío o calor"
  dificultad="🌿"
  datos={`temperatura = 20`}
  starter={`# Mostrá 'frío', 'templado' o 'calor'.\n`}
  tests={`assert correr(temperatura=10).strip() == "frío", "10 grados es frío"\nassert correr(temperatura=14.9).strip() == "frío", "justo abajo de 15 sigue siendo frío"\nassert correr(temperatura=15).strip() == "templado", "15 ya es templado"\nassert correr(temperatura=25).strip() == "templado", "25 todavía es templado"\nassert correr(temperatura=25.1).strip() == "calor", "pasando 25 es calor"\nassert correr(temperatura=30).strip() == "calor", "30 es calor"`}
  pistas={[
    "Los tests prueban 14.9, 15, 25 y 25.1 a propósito. ¿En cuál de los tres grupos cae exactamente el 15? ¿Y el 25?",
    "Si preguntás primero por el rango del medio, ¿qué pasa con un valor de 10?",
  ]}
>
Mostrá `frío` (menos de 15), `templado` (de 15 a 25 inclusive) o `calor` (más de 25).
</EjercicioPython>

<EjercicioPython
  titulo="Verificar contraseña"
  dificultad="🌿"
  datos={`CONTRASENA = "python123"\ningresada = "python123"`}
  starter={`# Compará 'ingresada' con 'CONTRASENA'.\n`}
  tests={`assert correr(ingresada="python123").strip() == "acceso concedido", "la contraseña correcta entra"\nassert correr(ingresada="Python123").strip() == "acceso denegado", "las mayúsculas importan"\nassert correr(ingresada="").strip() == "acceso denegado", "vacía no entra"\nassert correr(ingresada="python1234").strip() == "acceso denegado", "parecida no alcanza"`}
  pistas={[
    "Mirá el test de <code>Python123</code> con mayúscula: se espera que NO entre. ¿El operador que estás usando distingue mayúsculas de minúsculas?",
  ]}
>
Mostrá `acceso concedido` si `ingresada` coincide con `CONTRASENA`, o `acceso denegado` si no.
</EjercicioPython>

<EjercicioPython
  titulo="Comparar con 10"
  dificultad="🌿"
  datos={`numero = 15`}
  starter={`# Mostrá 'mayor', 'igual' o 'menor' respecto de 10.\n`}
  tests={`assert correr(numero=15).strip() == "mayor", "15 es mayor que 10"\nassert correr(numero=10).strip() == "igual", "10 es exactamente 10"\nassert correr(numero=3).strip() == "menor", "3 es menor"\nassert correr(numero=10.0).strip() == "igual", "10.0 también es igual a 10"`}
  pistas={[
    "Hay un caso que se escapa fácil si solo preguntás por mayor y menor. ¿Cuál es?",
  ]}
>
Mostrá `mayor`, `igual` o `menor` comparando `numero` con 10.
</EjercicioPython>

<EjercicioPython
  titulo="Boletín con promedio"
  dificultad="🌳"
  datos={`n1 = 9\nn2 = 8\nn3 = 7`}
  starter={`# Calculá el promedio de las tres notas y mostrá la condición.\n`}
  tests={`assert correr(n1=9, n2=9, n3=9).strip() == "promociona", "promedio 9"\nassert correr(n1=8, n2=8, n3=8).strip() == "promociona", "8 justo promociona"\nassert correr(n1=7, n2=7, n3=7).strip() == "aprueba", "promedio 7"\nassert correr(n1=6, n2=6, n3=6).strip() == "aprueba", "6 justo aprueba"\nassert correr(n1=5, n2=5, n3=5).strip() == "recupera", "promedio 5"\nassert correr(n1=10, n2=8, n3=6).strip() == "promociona", "el promedio es 8, no mires las notas sueltas"`}
  pistas={[
    "El último test tiene notas muy distintas entre sí. ¿Estás mirando cada nota por separado o la cuenta que las junta?",
    "¿En qué orden conviene preguntar por los cortes de 8 y 6 para que un promedio de 9 no caiga en el lugar equivocado?",
  ]}
>
Calculá el promedio de `n1`, `n2` y `n3`, y mostrá `promociona` (8 o más), `aprueba` (6 o más) o
`recupera` (menos de 6).
</EjercicioPython>

<EjercicioPython
  titulo="Entre 1 y 100"
  dificultad="🌿"
  datos={`numero = 50`}
  starter={`# Mostrá 'dentro' o 'fuera'.\n`}
  tests={`assert correr(numero=50).strip() == "dentro", "50 está en el rango"\nassert correr(numero=1).strip() == "dentro", "el 1 está incluido"\nassert correr(numero=100).strip() == "dentro", "el 100 está incluido"\nassert correr(numero=0).strip() == "fuera", "el 0 queda afuera"\nassert correr(numero=101).strip() == "fuera", "el 101 queda afuera"`}
  pistas={[
    "Los tests prueban justo el 1 y el 100: los dos tienen que dar dentro. ¿Los operadores que elegiste incluyen los extremos?",
    "Son dos condiciones que se tienen que cumplir a la vez. ¿Con qué las unís? (En Python también se puede escribir todo junto, como en matemática.)",
  ]}
>
Mostrá `dentro` si `numero` está entre 1 y 100 **incluidos**, o `fuera` si no.
</EjercicioPython>

## Un detalle que te va a ahorrar líneas

Cuando una condición ya es verdadera o falsa por sí misma, no hace falta el `if`:

```python
# En vez de esto...
if edad >= 18:
    es_mayor = True
else:
    es_mayor = False

# ...esto dice exactamente lo mismo
es_mayor = edad >= 18
```

`edad >= 18` **ya es** un `True` o un `False`. Envolverlo en un `if` para después guardar `True` o
`False` es dar una vuelta de más. Las dos versiones funcionan; la segunda se lee mejor.

---

Siguiente: [Listas](../listas/) — guardar muchos valores juntos en vez de una variable por cada uno.
