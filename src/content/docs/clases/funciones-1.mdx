---
title: Funciones I — definición, parámetros y retorno
description: Tu primera caja de herramientas reutilizable. Aprendé a definir funciones haciendo, con ejercicios que se verifican solos.
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_2_funciones_y_colecciones/funciones_1/" />


:::tip[Antes de empezar]
Esta clase es **interactiva**. Escribí el código en cada recuadro, tocá **▶ Ejecutar** para verlo
correr y **✓ Verificar** para que los tests te digan si está bien. La primera vez tarda unos segundos
en cargar Python. 🐍
:::

## El problema

Tenés que saludar a tres personas:

```python
print("¡Hola, Ana!")
print("¡Hola, Beto!")
print("¡Hola, Carla!")
```

Funciona... pero si mañana el saludo cambia a "¡Buenas, Ana!", tenés que tocar **las tres líneas**.
Y si fueran 100 personas, 100 líneas. Estás **repitiendo** la misma idea una y otra vez.

¿Y si pudieras guardar la idea de "saludar" en un solo lugar, ponerle nombre, y reusarla las veces
que quieras? Para eso existen las **funciones**.

## Qué es una función

Una función es un **bloque de código con nombre** que podés ejecutar (llamar) cuando quieras. Recibe
datos de entrada (**parámetros**) y puede devolver un resultado (**retorno**).

```python
def saludar(nombre):       # 'nombre' es el parámetro (la entrada)
    return f"¡Hola, {nombre}!"   # 'return' devuelve el resultado

mensaje = saludar("Ana")   # la llamamos con "Ana"
print(mensaje)             # ¡Hola, Ana!
```

Tres piezas clave:

- **`def`** + nombre + `(parámetros)` → así se **define**.
- **`return`** → el valor que la función **devuelve** a quien la llamó.
- **Llamarla**: `saludar("Ana")` ejecuta el bloque con `nombre = "Ana"`.

:::caution[`return` no es `print`]
`print` **muestra** algo en pantalla; `return` **devuelve** un valor para seguir usándolo. Una
función que solo hace `print` no te sirve para guardar el resultado en una variable.
:::

## Manos a la obra

:::note[Vas a ver `pass` en el código]
`pass` es un **marcador de lugar**: una línea que no hace nada. Está para que el código sea válido
mientras todavía no lo escribiste. **Borralo y poné tu solución** en su lugar.
:::

<EjercicioPython
  titulo="Tu primera función"
  dificultad="🌱"
  starter={`def saludar(nombre):\n    # Tiene que DEVOLVER el texto "¡Hola, <nombre>!"\n    pass`}
  tests={`assert saludar("Ana") == "¡Hola, Ana!", 'saludar("Ana") debería devolver "¡Hola, Ana!"'\nassert saludar("Beto") == "¡Hola, Beto!"\nprint("Probado con Ana y Beto")`}
  pistas={[
    "Escribí a mano el saludo para Ana y para Beto. ¿Qué parte del texto es siempre igual y cuál cambia según quién sea?",
    "El test compara el valor que sale de la función. ¿Alcanza con <em>mostrar</em> el saludo en pantalla, o hace falta <em>entregarlo</em> para que puedan compararlo?",
  ]}
>
Escribí una función `saludar(nombre)` que **devuelva** el texto `¡Hola, <nombre>!`.

Por ejemplo, `saludar("Ana")` tiene que devolver `"¡Hola, Ana!"`.
</EjercicioPython>

<EjercicioPython
  titulo="Más de un parámetro"
  dificultad="🌿"
  starter={`def area_rectangulo(base, altura):\n    pass`}
  tests={`assert area_rectangulo(3, 4) == 12, "el área de 3 x 4 debería ser 12"\nassert area_rectangulo(5, 2) == 10, "el área de 5 x 2 debería ser 10"\nassert area_rectangulo(0, 9) == 0, "el área de 0 x 9 debería ser 0"\nprint("Áreas calculadas OK")`}
  pistas={[
    "Si te dan la base y la altura de un rectángulo, ¿qué cuenta hacés para el área? Escribí esa misma cuenta con los nombres de los parámetros.",
    "¿En qué orden llegan los valores cuando alguien escribe <code>area(3, 4)</code>? ¿Cómo sabe la función cuál es la base?",
  ]}
>
Una función puede recibir **varios** parámetros. Escribí `area_rectangulo(base, altura)` que
**devuelva** el área (base por altura).

Ejemplo: `area_rectangulo(3, 4)` → `12`.
</EjercicioPython>

<EjercicioPython
  titulo="Funciones + listas"
  dificultad="🌿"
  starter={`def promedio(notas):\n    # 'notas' es una lista de números, ej: [10, 8, 9]\n    pass`}
  tests={`assert promedio([10, 8, 9]) == 9, "el promedio de [10, 8, 9] debería ser 9"\nassert promedio([4, 6]) == 5, "el promedio de [4, 6] debería ser 5"\nassert round(promedio([7, 8, 10]), 2) == 8.33, "el promedio de [7, 8, 10] debería ser 8.33"\nprint("Promedios verificados")`}
  pistas={[
    "¿Cómo sacás un promedio a mano? Nombrá las dos cosas que necesitás saber sobre la lista antes de poder dividir.",
    "¿Qué pasaría si la lista viniera vacía? ¿Qué número estarías usando como divisor?",
  ]}
>
En Bucles ya calculaste un promedio. Pero aquel código servía para **una** lista: si querías el
promedio de otra, lo copiabas y pegabas. Convertirlo en función es lo que lo vuelve reutilizable —
y de paso, verificable.

Escribí `promedio(notas)`, que recibe una **lista de números** y **devuelve** su promedio.

Ejemplo: `promedio([10, 8, 9])` → `9`.
</EjercicioPython>

## Para llevar

- `def nombre(parametros):` define una función.
- `return` devuelve un valor (distinto de `print`).
- Definís una vez, la llamás todas las veces que quieras → **menos repetición, menos errores**.

## Más práctica

<EjercicioPython
  titulo="La calculadora"
  dificultad="🌿"
  starter={`def sumar(a, b):\n    pass\n\ndef restar(a, b):\n    pass\n\ndef multiplicar(a, b):\n    pass\n\ndef dividir(a, b):\n    pass`}
  tests={`assert sumar(2, 3) == 5, "2 + 3"\nassert restar(10, 4) == 6, "10 - 4"\nassert multiplicar(3, 4) == 12, "3 x 4"\nassert dividir(10, 2) == 5, "10 / 2"\nassert dividir(7, 2) == 3.5, "la división puede dar decimales"\nassert dividir(5, 0) is None, "dividir por cero tiene que devolver None, no romper"\nassert dividir(0, 5) == 0, "0 dividido cualquier cosa es 0 — ojo con confundir los casos"`}
  pistas={[
    "Tres de las cuatro son de una línea. La cuarta tiene un caso especial: ¿qué pasa en matemática si dividís por cero?",
    "Mirá los dos últimos tests: uno divide POR cero y el otro divide AL cero. ¿Cuál de los dos es el problemático?",
  ]}
>
Completá las cuatro funciones.

En `dividir`, si el divisor es `0`, devolvé `None` en lugar de romper.
</EjercicioPython>

<EjercicioPython
  titulo="Conversiones y chequeos"
  dificultad="🌱"
  starter={`def celsius_a_fahrenheit(c):\n    pass\n\ndef es_par(n):\n    pass\n\ndef valor_absoluto(n):\n    pass`}
  tests={`assert celsius_a_fahrenheit(0) == 32, "0 °C son 32 °F"\nassert celsius_a_fahrenheit(100) == 212, "100 °C son 212 °F"\nassert celsius_a_fahrenheit(-40) == -40, "-40 es el punto donde las dos escalas coinciden"\nassert es_par(4) is True, "4 es par"\nassert es_par(7) is False, "7 no es par"\nassert es_par(0) is True, "el 0 es par"\nassert valor_absoluto(5) == 5, "el absoluto de un positivo es él mismo"\nassert valor_absoluto(-5) == 5, "el absoluto de -5 es 5"\nassert valor_absoluto(0) == 0, "el absoluto de 0 es 0"`}
  pistas={[
    "La fórmula de Celsius a Fahrenheit es <code>c × 9/5 + 32</code>. ¿En qué orden se resuelven esas operaciones?",
    "En <code>es_par</code> los tests usan <code>is True</code>, no <code>== True</code>: tenés que devolver el booleano en sí. ¿Hace falta un <code>if</code> para eso?",
    "El valor absoluto solo cambia algo cuando el número es negativo. ¿Qué le hacés a un negativo para volverlo positivo?",
  ]}
>
Completá las tres funciones:

- `celsius_a_fahrenheit(c)` → convierte grados
- `es_par(n)` → `True` o `False`
- `valor_absoluto(n)` → el número sin signo
</EjercicioPython>

<EjercicioPython
  titulo="Analizar una lista"
  dificultad="🌳"
  starter={`def analizar(numeros):\n    pass`}
  tests={`assert analizar([1, 2, 3, 4]) == (1, 4, 2.5, 2), f"con [1,2,3,4] devolviste {analizar([1, 2, 3, 4])!r}"\nassert analizar([5]) == (5, 5, 5.0, 0), "un solo número impar"\nassert analizar([2]) == (2, 2, 2.0, 1), "un solo número par"\nassert analizar([-3, -1]) == (-3, -1, -2.0, 0), "con negativos también"\nminimo, maximo, promedio, pares = analizar([1, 2, 3, 4])\nassert (minimo, maximo, promedio, pares) == (1, 4, 2.5, 2), "tiene que poder desempaquetarse en cuatro"`}
  pistas={[
    "Son cuatro valores en un orden fijo. Antes de escribir nada, nombralos y fijate cuál de ellos necesita recorrer la lista.",
    "Tres de los cuatro salen de funciones que ya conocés. ¿Cuál es el que sí o sí tenés que contar vos?",
  ]}
>
Completá `analizar(numeros)` para que devuelva el mínimo, el máximo, el promedio y la **cantidad de
números pares**, en ese orden.

:::note[Devolver varios valores de una]
`return a, b, c` devuelve las tres cosas juntas, separadas por coma. Lo que sale se llama **tupla** y
tiene su propia clase más adelante; por ahora alcanza con saber que se escribe así y que del otro lado
se puede desarmar: `minimo, maximo, promedio, pares = analizar(notas)`.
:::

`analizar([1, 2, 3, 4])` → `(1, 4, 2.5, 2)`
</EjercicioPython>

<EjercicioPython
  titulo="Validador de contraseñas"
  dificultad="🌳"
  starter={`def validar_password(password):\n    pass`}
  tests={`assert validar_password("Python123") is True, "cumple todas las reglas"\nassert validar_password("python123") is False, "le falta la mayúscula"\nassert validar_password("Python") is False, "le falta el número"\nassert validar_password("Py1") is False, "es muy corta"\nassert validar_password("Python 123") is False, "tiene un espacio"\nassert validar_password("ABCDEFG1") is True, "8 caracteres justos, con mayúscula y número"`}
  pistas={[
    "Son cuatro condiciones que se tienen que cumplir TODAS. ¿Con qué las unís?",
    "Para saber si hay <em>al menos una</em> mayúscula tenés que mirar carácter por carácter. ¿Qué método te dice si un carácter suelto es mayúscula?",
    "El test de <code>ABCDEFG1</code> tiene exactamente 8 caracteres y tiene que pasar. ¿Tu condición de largo incluye al 8 o lo deja afuera?",
  ]}
>
Completá `validar_password` para que devuelva `True` solo si la contraseña cumple **todas** estas
reglas:

- Al menos **8 caracteres**
- Al menos **una mayúscula**
- Al menos **un número**
- **Sin espacios**
</EjercicioPython>

:::note[¿Qué sigue?]
En **Funciones II** vamos a ver el *scope* (dónde "viven" las variables), los valores por defecto y
cómo una función puede recibir una cantidad variable de argumentos.
:::
