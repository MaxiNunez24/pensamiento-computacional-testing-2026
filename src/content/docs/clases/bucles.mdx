---
title: Bucles — while y for
description: "Repetir sin copiar y pegar: cuándo usar cada uno, acumuladores y cómo no colgar el programa."
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_1_fundamentos/while/" />

## El problema

Contar del 1 al 5 se puede hacer así:

```python
print(1)
print(2)
print(3)
print(4)
print(5)
```

¿Y del 1 al 1000? No vas a escribir mil líneas. Y peor: si mañana hay que cambiar algo, hay que
cambiarlo mil veces.

Un **bucle** te deja escribir la instrucción **una vez** y decirle a Python cuántas veces repetirla.

## `while`: repetir mientras se cumpla algo

```python
numero = 1

while numero <= 5:
    print(numero)
    numero = numero + 1
```

Se lee literal: *"mientras `numero` sea menor o igual a 5, mostralo y sumale 1"*.

Un `while` tiene siempre tres partes, y si te falta alguna se rompe:

1. **Antes**: preparar la variable (`numero = 1`).
2. **La condición**: hasta cuándo seguir (`numero <= 5`).
3. **Adentro**: hacer algo **y acercarse al final** (`numero = numero + 1`).

:::caution[El bucle infinito]
Si te olvidás el paso 3, la condición nunca se vuelve falsa y el programa **no termina nunca**:

```python
numero = 1
while numero <= 5:
    print(numero)      # 💥 imprime 1 para siempre
```

Acá no pasa nada grave: a los 15 segundos la página corta la ejecución sola y te avisa. En tu compu
tendrías que frenarlo con `Ctrl + C`.
:::

<EjercicioPython
  titulo="La cuenta regresiva"
  dificultad="🌿"
  datos={`desde = 5`}
  starter={`# Mostrá la cuenta regresiva desde 'desde' hasta 1,\n# un número por línea.\n`}
  tests={`assert correr(desde=5).split() == ["5","4","3","2","1"], f'con 5 imprimiste {correr(desde=5).split()!r}'\nassert correr(desde=1).split() == ["1"], "desde 1, solo el 1"\nassert correr(desde=0).strip() == "", "desde 0 no hay nada que contar: no imprimas nada"`}
  pistas={[
    "Contá en voz alta desde 5 hasta 1. ¿En qué momento dejás de contar? Esa es tu condición.",
    "¿El número de cada vuelta crece o decrece? ¿Qué línea de tu bucle se encarga de eso?",
    "Mirá el test de <code>desde = 0</code>: no tiene que imprimir nada. ¿Tu bucle llega a entrar aunque sea una vez, o directamente no entra?",
  ]}
>
Mostrá los números desde `desde` hasta 1, de mayor a menor, **uno por línea**.
</EjercicioPython>

## `for`: repetir una vez por cada elemento

Cuando ya sabés **sobre qué** querés repetir (una lista, un texto, un rango de números), el `for` es
más corto y no se puede colgar:

```python
for fruta in ["manzana", "pera", "uva"]:
    print(fruta)
```

Se lee: *"para cada fruta en esa lista, mostrala"*. No hay contador que actualizar ni condición que
se te olvide: se termina solo cuando no quedan elementos.

### `range()`: para contar

```python
for i in range(5):        # 0, 1, 2, 3, 4
    print(i)

for i in range(1, 6):     # 1, 2, 3, 4, 5
    print(i)

for i in range(0, 10, 2): # 0, 2, 4, 6, 8
    print(i)
```

`range` **arranca en 0** si no le decís otra cosa, y **nunca llega al número final**. Eso último
sorprende al principio, pero es lo que hace que `range(len(lista))` dé exactamente las posiciones
válidas.

:::tip[¿Cuál uso?]
- ¿Sabés de antemano cuántas vueltas son, o tenés una lista? → **`for`**
- ¿Depende de algo que pasa adentro y no sabés cuándo va a terminar? → **`while`**

En la práctica el `for` cubre la gran mayoría de los casos.
:::

## Acumuladores: el patrón que más vas a usar

Sumar todos los números de una lista es siempre la misma receta:

```python
total = 0                  # 1. arrancar en un valor neutro
for numero in [10, 20, 30]:
    total = total + numero # 2. actualizar en cada vuelta
print(total)               # 3. usar el resultado al final  → 60
```

Ese `total` se llama **acumulador**. La misma idea sirve para contar cosas (arrancás en 0 y sumás 1),
o para armar un texto (arrancás en `""` y vas pegando).

<EjercicioPython
  titulo="El promedio"
  dificultad="🌿"
  datos={`notas = [10, 8, 6]`}
  starter={`# Mostrá el promedio de 'notas'.\n`}
  tests={`assert correr(notas=[10, 8, 6]).strip() == "8.0", f'con [10, 8, 6] imprimiste {correr(notas=[10, 8, 6]).strip()!r}'\nassert correr(notas=[5]).strip() == "5.0", "el promedio de una sola nota es esa nota"\nassert correr(notas=[4, 5]).strip() == "4.5", "el promedio puede tener decimales"\nassert correr(notas=[]).strip() == "0", "con la lista vacía mostrá 0, no rompas"`}
  pistas={[
    "¿Cómo sacás un promedio a mano? Nombrá los dos números que necesitás antes de poder dividir.",
    "Mirá el test de la lista vacía. Si intentás dividir por la cantidad de notas y no hay ninguna, ¿qué pasa? ¿Cómo evitás llegar a esa división?",
  ]}
>
Mostrá el promedio de `notas`. Si la lista está **vacía**, mostrá `0`.
</EjercicioPython>

<EjercicioPython
  titulo="Contar los aprobados"
  dificultad="🌿"
  datos={`notas = [10, 3, 7, 2, 6]`}
  starter={`# Mostrá cuántas notas son 6 o más.\n`}
  tests={`assert correr(notas=[10, 3, 7, 2, 6]).strip() == "3", f'imprimiste {correr(notas=[10, 3, 7, 2, 6]).strip()!r}'\nassert correr(notas=[1, 2, 3]).strip() == "0", "ninguna llega a 6"\nassert correr(notas=[]).strip() == "0", "sin notas, ningún aprobado"\nassert correr(notas=[6, 6, 6]).strip() == "3", "el 6 aprueba"`}
  pistas={[
    "Acá se juntan dos cosas que ya viste: recorrer todas las notas, y decidir por cada una si cuenta o no. ¿Cuál de las dos va adentro de la otra?",
    "Si contaras a mano con el dedo, ¿en qué número arrancás antes de mirar la primera nota?",
  ]}
>
Mostrá **cuántas** notas de la lista son 6 o más.
</EjercicioPython>

## `break` y `continue`

Dos atajos para cuando el bucle tiene que cortarse antes:

```python
for numero in [1, 5, 8, 3]:
    if numero == 8:
        break        # corta el bucle acá, no sigue
    print(numero)    # 1, 5

for numero in [1, 2, 3, 4]:
    if numero % 2 == 0:
        continue     # saltea esta vuelta, sigue con la próxima
    print(numero)    # 1, 3
```

- **`break`** → me voy del bucle.
- **`continue`** → esta vuelta no me interesa, seguí con la siguiente.

<EjercicioPython
  titulo="El primero que aparece"
  dificultad="🌳"
  datos={`numeros = [3, 8, -2, 5, -9]`}
  starter={`# Mostrá el primer número negativo.\n# Si no hay ninguno, mostrá 'ninguno'.\n`}
  tests={`assert correr(numeros=[3, 8, -2, 5, -9]).strip() == "-2", f'esperaba -2 (el primero), imprimiste {correr(numeros=[3, 8, -2, 5, -9]).strip()!r}'\nassert correr(numeros=[1, 2, 3]).strip() == "ninguno", "si no hay negativos, mostrá ninguno"\nassert correr(numeros=[-1]).strip() == "-1", "el único elemento es negativo"\nassert correr(numeros=[]).strip() == "ninguno", "lista vacía"`}
  pistas={[
    "El test espera -2 y no -9, aunque los dos son negativos. ¿Qué tiene que hacer tu bucle apenas encuentra el primero?",
    "Si recorrés la lista entera y nunca encontraste ninguno, ¿cómo te enterás al final de que no hubo? ¿Qué te convendría anotar antes de empezar?",
  ]}
>
Mostrá **el primer** número negativo de `numeros`. Si no hay ninguno, mostrá `ninguno`.
</EjercicioPython>


## Más práctica

<EjercicioPython
  titulo="Del 1 al 10"
  dificultad="🌱"
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert salida.split() == [str(n) for n in range(1, 11)], f"imprimiste {salida.split()!r}"`}
  pistas={[
    "¿Con qué número arrancás y hasta cuál seguís? Esos dos son la preparación y la condición.",
    "Si tu programa no termina nunca, ¿qué le falta a la variable dentro del bucle?",
  ]}
>
Mostrá los números del **1 al 10**, uno por línea, usando `while`.
</EjercicioPython>

<EjercicioPython
  titulo="Solo los pares"
  dificultad="🌱"
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert salida.split() == [str(n) for n in range(2, 21, 2)], f"imprimiste {salida.split()!r}"`}
  pistas={[
    "Hay dos caminos: recorrer todos y filtrar los pares, o saltar directamente de dos en dos. ¿Cuál te ahorra el <code>if</code>?",
  ]}
>
Mostrá los números **pares** del 2 al 20, uno por línea.
</EjercicioPython>

<EjercicioPython
  titulo="Suma del 1 al 100"
  dificultad="🌱"
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert salida.strip() == "5050", f"imprimiste {salida.strip()!r}"`}
  pistas={[
    "Es el patrón acumulador. ¿En qué valor arranca el total antes de sumar el primer número?",
    "¿El <code>print</code> va adentro del bucle o después? Fijate que el test espera un solo número.",
  ]}
>
Sumá todos los números del 1 al 100 y mostrá **solo el resultado**.
</EjercicioPython>

<EjercicioPython
  titulo="Pares hasta N"
  dificultad="🌿"
  entradas={`10`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(entradas=["10"]).strip().splitlines()[-6:] == ["0","2","4","6","8","10"], f'con 10 terminaste con {correr(entradas=["10"]).strip().splitlines()[-6:]!r}'\nassert correr(entradas=["5"]).strip().splitlines()[-3:] == ["0","2","4"], "con 5 el último par es el 4"\nassert correr(entradas=["0"]).strip().splitlines()[-1] == "0", "con 0 el único par es el 0"`}
  pistas={[
    "El 0 también es par y tiene que aparecer. ¿Desde qué número arrancás?",
    "Con N = 5 el último par es el 4, no el 5. ¿Tu condición deja pasar números mayores que N?",
  ]}
>
Pedí un número con `input()` y mostrá todos los **pares desde 0 hasta ese número** (incluido si es par).
</EjercicioPython>

<EjercicioPython
  titulo="Suma hasta el cero"
  dificultad="🌳"
  entradas={`5\n3\n0`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(entradas=["5","3","0"]).strip().splitlines()[-1] == "Ingresaste 2 números y suman 8", f'terminaste con {correr(entradas=["5","3","0"]).strip().splitlines()[-1]!r}'\nassert correr(entradas=["0"]).strip().splitlines()[-1] == "Ingresaste 0 números y suman 0", "si el primero es 0, no se cuenta nada"\nassert correr(entradas=["10","-4","0"]).strip().splitlines()[-1] == "Ingresaste 2 números y suman 6", "los negativos también cuentan"`}
  pistas={[
    "El 0 marca el final y NO se cuenta. ¿Cómo hacés para leer un número, revisarlo, y recién ahí decidir si seguís?",
    "Fijate en el test donde el primer número ya es 0: no tiene que contar nada. ¿Tu bucle llega a ejecutarse una vez de más?",
  ]}
>
Pedí números hasta que ingresen un **0**. Al final mostrá una línea con este formato exacto:

```
Ingresaste 2 números y suman 8
```

El 0 marca el final y **no se cuenta**.
</EjercicioPython>

<EjercicioPython
  titulo="El cajero automático"
  dificultad="🌳"
  datos={`saldo = 1000`}
  entradas={`300\n800\n200\n0`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr(entradas=["300","800","200","0"])\nassert r.strip().splitlines()[-1] == "Saldo final: 500", f'terminaste con {r.strip().splitlines()[-1]!r}'\nassert "insuficiente" in r.lower(), "cuando el retiro supera el saldo tenés que avisar que es insuficiente"\nassert correr(entradas=["0"]).strip().splitlines()[-1] == "Saldo final: 1000", "si no retira nada, el saldo queda igual"\nassert correr(entradas=["1000","0"]).strip().splitlines()[-1] == "Saldo final: 0", "puede retirar todo"`}
  pistas={[
    "Son dos decisiones distintas por vuelta: si el retiro es válido, y si hay que seguir. ¿Cuál corta el bucle y cuál solo evita descontar?",
    "En el caso del retiro que no alcanza, ¿el saldo tiene que cambiar en algo?",
  ]}
>
El saldo arranca en `$1000`. Pedí cuánto quieren retirar, una y otra vez, hasta que ingresen `0`.

- Si el retiro **supera el saldo**, avisá que es *insuficiente* y no descuentes nada.
- Al terminar, mostrá `Saldo final: 500` (con el saldo que haya quedado).
</EjercicioPython>

<EjercicioPython
  titulo="Adiviná el número"
  dificultad="🌳"
  datos={`secreto = 42`}
  entradas={`50\n30\n42`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr(entradas=["50","30","42"])\nassert r.strip().splitlines()[-1] == "Adivinaste en 3 intentos", f'terminaste con {r.strip().splitlines()[-1]!r}'\nassert correr(entradas=["42"]).strip().splitlines()[-1] == "Adivinaste en 1 intentos", "si acierta de una, es 1 intento"\nr2 = correr(entradas=["10","42"]).lower()\nassert "alto" in r2 or "mayor" in r2, "si el intento es menor al secreto, hay que decirle que apunte más alto"`}
  pistas={[
    "El bucle no sabe de antemano cuántas vueltas son: depende de cuándo acierten. ¿Qué tipo de bucle sirve para eso?",
    "Cada vuelta tenés que hacer dos cosas además de comparar: llevar la cuenta, y decidir si seguís. ¿Dónde va cada una?",
  ]}
>
El número secreto es **42**. Pedí intentos hasta que lo adivinen.

- Si el intento es **menor**, decile que apunte más **alto**.
- Si es **mayor**, que apunte más **bajo**.
- Al acertar, mostrá `Adivinaste en 3 intentos`.
</EjercicioPython>

<EjercicioPython
  titulo="Estadísticas de notas"
  dificultad="🌳"
  entradas={`8\n6\n10\n-1`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr(entradas=["8","6","10","-1"]).strip().splitlines()[-4:]\nassert r == ["Cantidad: 3", "Promedio: 8.0", "Mayor: 10", "Menor: 6"], f"terminaste con {r!r}"\nr2 = correr(entradas=["7","-1"]).strip().splitlines()[-4:]\nassert r2 == ["Cantidad: 1", "Promedio: 7.0", "Mayor: 7", "Menor: 7"], f"con una sola nota: {r2!r}"`}
  pistas={[
    "Necesitás cuatro cosas al final. ¿Cuáles se pueden ir calculando en cada vuelta y cuál se saca recién al terminar?",
    "Para saber cuál fue la mayor, ¿con qué valor te conviene arrancar la comparación para que la primera nota siempre gane?",
  ]}
>
Pedí notas hasta que ingresen **-1**. Al final mostrá exactamente estas cuatro líneas:

```
Cantidad: 3
Promedio: 8.0
Mayor: 10
Menor: 6
```

El -1 marca el final y no cuenta como nota.
</EjercicioPython>

<EjercicioPython
  titulo="Letra por letra"
  dificultad="🌱"
  entradas={`Tux`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(entradas=["Tux"]).strip().splitlines()[-3:] == ["T","u","x"], f'con Tux terminaste con {correr(entradas=["Tux"]).strip().splitlines()[-3:]!r}'\nassert correr(entradas=["a"]).strip().splitlines()[-1] == "a", "con una sola letra"`}
  pistas={[
    "Un <code>for</code> recorre los elementos de una colección. ¿Un texto se puede recorrer igual que una lista? Probá y fijate.",
  ]}
>
Pedí un nombre y mostrá **cada letra en una línea distinta**.
</EjercicioPython>

<EjercicioPython
  titulo="Factorial"
  dificultad="🌿"
  entradas={`5`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(entradas=["5"]).strip().splitlines()[-1] == "120", f'el factorial de 5 es 120, imprimiste {correr(entradas=["5"]).strip().splitlines()[-1]!r}'\nassert correr(entradas=["1"]).strip().splitlines()[-1] == "1", "el factorial de 1 es 1"\nassert correr(entradas=["0"]).strip().splitlines()[-1] == "1", "el factorial de 0 es 1 por definición"`}
  pistas={[
    "El factorial de 5 es 5 × 4 × 3 × 2 × 1. Es un acumulador, pero no de sumas. ¿Con qué valor tiene que arrancar para no arruinar la cuenta?",
    "Mirá el test del 0: tiene que dar 1. ¿Tu bucle llega a entrar en ese caso? ¿Y qué queda en el acumulador si no entra?",
  ]}
>
Pedí un número y mostrá su **factorial**.

El factorial de 5 es `5 × 4 × 3 × 2 × 1 = 120`.
</EjercicioPython>

<EjercicioPython
  titulo="Tabla de multiplicar"
  dificultad="🌿"
  entradas={`3`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr(entradas=["3"]).strip().splitlines()[-10:]\nassert r == [f"3 x {i} = {3*i}" for i in range(1, 11)], f"terminaste con {r!r}"\nr2 = correr(entradas=["7"]).strip().splitlines()[-10:]\nassert r2 == [f"7 x {i} = {7*i}" for i in range(1, 11)], "con otro número la tabla tiene que acompañar"`}
  pistas={[
    "El formato es exacto: <code>3 x 1 = 3</code>, con espacios alrededor de la x y del igual. ¿Cómo armás un texto que mezcle números y símbolos?",
    "La tabla va del 1 al 10. ¿Tu <code>range</code> llega hasta el 10 inclusive?",
  ]}
>
Pedí un número y mostrá su **tabla de multiplicar** del 1 al 10, con este formato exacto:

```
3 x 1 = 3
3 x 2 = 6
...
```
</EjercicioPython>

<EjercicioPython
  titulo="Triángulo de asteriscos"
  dificultad="🌳"
  entradas={`4`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(entradas=["4"]).strip().splitlines()[-4:] == ["*","**","***","****"], f'con 4 terminaste con {correr(entradas=["4"]).strip().splitlines()[-4:]!r}'\nassert correr(entradas=["1"]).strip().splitlines()[-1] == "*", "con 1 fila es un solo asterisco"`}
  pistas={[
    "La fila 1 tiene 1 asterisco, la fila 2 tiene 2… ¿qué relación hay entre el número de vuelta y la cantidad?",
    "¿Hace falta un segundo bucle adentro para repetir el asterisco, o hay una forma más corta de repetir un texto?",
  ]}
>
Pedí un número `n` y mostrá un triángulo de `n` filas:

```
*
**
***
****
```
</EjercicioPython>

## Recorrer listas: los tres patrones

Estos tres se repiten toda la vida programando. Ahora que tenés `for` y listas, son tuyos.

<EjercicioPython
  titulo="Transformar una lista"
  dificultad="🌱"
  starter={`numeros = [1, 2, 3]\n\n# Creá una lista 'dobles' con cada número multiplicado por 2\n`}
  tests={`assert dobles == [2, 4, 6], f"dobles debería ser [2, 4, 6] y quedó {dobles!r}"\nprint("¡Lista duplicada!")`}
  pistas={[
    "Si tuvieras que hacerlo a mano con lápiz: ¿arrancás con una hoja en blanco o ya escribiste algo? ¿Qué representa esa hoja en tu código?",
    "Por cada número de la lista original vas a producir uno nuevo. ¿Dónde lo vas guardando para no perderlo?",
  ]}
>
Tenés la lista `numeros = [1, 2, 3]`. Creá una lista llamada **`dobles`** con cada número
multiplicado por 2.

Resultado esperado: `dobles` debería ser `[2, 4, 6]`.
</EjercicioPython>

<EjercicioPython
  titulo="Filtrar elementos"
  dificultad="🌿"
  starter={`numeros = [1, 2, 3, 4, 5, 6]\n\n# Creá una lista 'pares' con solo los números pares de 'numeros'\n`}
  tests={`assert pares == [2, 4, 6], f"pares debería ser [2, 4, 6] y quedó {pares!r}"\nprint("¡Pares filtrados!")`}
  pistas={[
    "¿Cómo te das cuenta de que un número es par sin usar la calculadora? ¿Qué te tiene que dar la división por 2?",
    "Esta vez no todos los elementos pasan a la lista nueva. ¿En qué momento del recorrido decidís si este entra o no?",
  ]}
>
Tenés la lista `numeros = [1, 2, 3, 4, 5, 6]`. Creá una lista **`pares`** con **solo los números
pares** (en el mismo orden).

Resultado esperado: `pares` debería ser `[2, 4, 6]`.
</EjercicioPython>

<EjercicioPython
  titulo="Mayores al promedio"
  dificultad="🌳"
  datos={`notas = [4, 8, 6, 10]`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`import ast\nassert ast.literal_eval(correr().strip().splitlines()[-1]) == [8, 10], f"salió {correr().strip().splitlines()[-1]!r}"\nassert ast.literal_eval(correr(notas=[5, 5, 5]).strip().splitlines()[-1]) == [], "si son todas iguales, ninguna supera el promedio"\nassert ast.literal_eval(correr(notas=[1, 9]).strip().splitlines()[-1]) == [9], "solo el 9 supera el promedio de 5"`}
  pistas={[
    "Necesitás el promedio ANTES de poder comparar. ¿Se puede calcular mientras recorrés, o hay que sacarlo primero?",
    "Fijate en el test donde todas las notas son iguales: ninguna tiene que aparecer. ¿Estás usando mayor, o mayor o igual?",
  ]}
>
Mostrá una lista con las notas que están **por encima del promedio**.

Con `[4, 8, 6, 10]` (promedio 7) tiene que dar `[8, 10]`.
</EjercicioPython>

:::note[Guardate estos tres]
Transformar, filtrar y acumular es el 90% de lo que se hace con listas. En la clase que sigue vas a
ver que los dos primeros se pueden escribir **en una sola línea** — y vas a volver sobre estos mismos
ejercicios para comprobarlo. Por ahora, que te salgan con `for`: entender el mecanismo primero es lo
que después te deja leer la versión corta sin marearte.
:::

---

Siguiente: [Cuadernillo de listas](../cuadernillo-listas/) — a practicar todo junto: listas, condicionales y bucles.
