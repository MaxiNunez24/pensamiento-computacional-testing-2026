---
title: Variables y tipos de datos
description: 'Guardar valores para reusarlos, los cuatro tipos básicos de Python y por qué "10" no es lo mismo que 10.'
---

import EjercicioPython from '../../../components/EjercicioPython.astro';
import VolverATeoria from '../../../components/VolverATeoria.astro';

<VolverATeoria ruta="clases/bloque_1_fundamentos/variables/" />

## El problema

Mirá este precio repetido por todos lados:

```python
print("El café sale 1500")
print("Dos cafés salen", 1500 * 2)
print("Con 10% de descuento:", 1500 * 0.9)
```

Si mañana el café aumenta, tenés que cazar **cada** `1500` y cambiarlo. Si te olvidás uno, el programa
queda mintiendo y no te avisa.

Una **variable** es un nombre para un valor. Lo escribís una vez y lo usás donde quieras:

```python
precio_cafe = 1500

print("El café sale", precio_cafe)
print("Dos cafés salen", precio_cafe * 2)
print("Con 10% de descuento:", precio_cafe * 0.9)
```

Ahora el precio vive en **un solo lugar**. Eso es lo que ganás: no repetir, y tener un lugar único
donde cambiar las cosas.

## Cómo se crea

```python
nombre_de_la_variable = valor
```

El `=` **no** es "igual" como en matemática: es **"guardá esto acá"**. Se lee de derecha a izquierda.

```python
edad = 30        # guardá 30 en edad
edad = edad + 1  # tomá lo que hay en edad, sumale 1, y guardalo de nuevo en edad
```

Esa segunda línea en matemática sería absurda. En programación es de lo más común.

<EjercicioPython
  titulo="Un solo lugar para cambiar"
  dificultad="🌱"
  datos={`precio = 1500\ncantidad = 2`}
  starter={`# 'precio' y 'cantidad' ya tienen valor.\n# Mostrá cuánto sale el total.\n`}
  tests={`assert correr(precio=1500, cantidad=2).strip() == "3000", f'con 1500 x 2 imprimiste {correr(precio=1500, cantidad=2).strip()!r}'\nassert correr(precio=800, cantidad=5).strip() == "4000", "con 800 x 5 tendría que dar 4000"\nassert correr(precio=1000, cantidad=0).strip() == "0", "cero cafés cuestan 0"`}
  pistas={[
    "Si te preguntan cuánto salen 3 cafés de $500, ¿qué cuenta hacés? Escribí esa misma cuenta con los nombres de las variables en lugar de los números.",
  ]}
>
Mostrá con `print()` cuánto salen `cantidad` cafés a `precio` cada uno.
</EjercicioPython>

## Los cuatro tipos básicos

Todo valor en Python es de algún **tipo**. Al principio te alcanza con cuatro:

| Tipo | Qué guarda | Ejemplos |
|------|-----------|----------|
| `int` | Números enteros | `30`, `-5`, `0` |
| `float` | Números con decimales | `1.5`, `-0.75`, `3.0` |
| `str` | Texto (*string*) | `"hola"`, `"30"`, `""` |
| `bool` | Verdadero o falso | `True`, `False` |

Podés preguntarle a Python de qué tipo es algo con `type()`:

```python
print(type(30))        # <class 'int'>
print(type("30"))      # <class 'str'>
print(type(1.5))       # <class 'float'>
print(type(True))      # <class 'bool'>
```

## `"30"` no es `30`

Esta distinción causa la mitad de los errores del primer mes, así que vale la pena frenar acá.

```python
30 + 30       # 60      → dos números: los suma
"30" + "30"   # "3030"  → dos textos: los pega uno atrás del otro
"30" + 30     # 💥 TypeError
```

El `30` con comillas es el **dibujo** del número: sirve para mostrarlo, no para hacer cuentas.

Para pasar de uno a otro, se **convierte**:

```python
int("30")     # 30      texto → entero
str(30)       # "30"    entero → texto
float("1.5")  # 1.5     texto → decimal
```

:::caution[Convertir puede fallar]
```python
int("hola")   # 💥 ValueError: no hay ningún número ahí adentro
int("3.5")    # 💥 ValueError: eso es un decimal, no un entero
float("3.5")  # ✅ 3.5
```
:::

<EjercicioPython
  titulo="Sumar lo que vino como texto"
  dificultad="🌿"
  datos={`a = "30"\nb = "12"`}
  starter={`# 'a' y 'b' son NÚMEROS ESCRITOS COMO TEXTO.\n# Mostrá su suma.\n`}
  tests={`assert correr(a="30", b="12").strip() == "42", f'imprimiste {correr(a="30", b="12").strip()!r} — ojo, "30" + "12" no da 42'\nassert correr(a="0", b="0").strip() == "0", "cero más cero es 0"\nassert correr(a="-5", b="5").strip() == "0", "los negativos también tienen que andar"`}
  pistas={[
    "Probá <code>print(a + b)</code> tal cual y mirá qué sale. ¿Por qué da eso y no 42?",
    "Antes de poder sumarlos como números, ¿qué les tendría que pasar a esos dos valores?",
  ]}
>
`a` y `b` son dos números **escritos como texto**. Mostrá su **suma** como número.

Con `a = "30"` y `b = "12"` tiene que aparecer `42`, no `3012`.
</EjercicioPython>

## Cómo nombrar variables

Las reglas que Python te obliga:

- Empiezan con letra o `_`, nunca con número.
- Sin espacios ni acentos ni guiones: `precio_cafe`, no `precio café` ni `precio-cafe`.
- Distingue mayúsculas: `edad` y `Edad` son **dos variables distintas**.

Y la regla que no te obliga pero importa más:

:::tip[El nombre es para la persona que lee]
```python
x = 1500        # ¿1500 qué?
precio_cafe = 1500   # ah, listo
```
En Python se estila `snake_case`: todo en minúscula, palabras separadas por `_`. El código se lee
muchas más veces de las que se escribe, y casi siempre lo lee tu yo de dentro de dos semanas.
:::

## Operadores

```python
7 + 2    # 9    suma
7 - 2    # 5    resta
7 * 2    # 14   multiplicación
7 / 2    # 3.5  división (siempre da float)
7 // 2   # 3    división entera (se queda con la parte entera)
7 % 2    # 1    resto de la división
7 ** 2   # 49   potencia
```

Los dos raros, `//` y `%`, son mucho más útiles de lo que parecen: `%` te dice si un número es
múltiplo de otro (si el resto es 0), y juntos sirven para partir cantidades.

<EjercicioPython
  titulo="Repartir sin romper nada"
  dificultad="🌿"
  datos={`caramelos = 10\nchicos = 3`}
  starter={`# Repartí los caramelos entre los chicos.\n# Mostrá: "3 para cada uno, sobran 1"\n`}
  tests={`assert correr(caramelos=10, chicos=3).strip() == "3 para cada uno, sobran 1", f'imprimiste {correr(caramelos=10, chicos=3).strip()!r}'\nassert correr(caramelos=9, chicos=3).strip() == "3 para cada uno, sobran 0", "9 entre 3 reparte justo"\nassert correr(caramelos=2, chicos=5).strip() == "0 para cada uno, sobran 2", "si no alcanza para uno cada uno, sobran todos"`}
  pistas={[
    "Repartí 10 caramelos entre 3 chicos en la cabeza. ¿Cuántos le tocan a cada uno y cuántos te quedan en la mano?",
    "La división normal de 10 entre 3 da 3.33, pero no podés dar un tercio de caramelo. ¿Cuál de los operadores te da solo la parte entera, y cuál te da lo que sobra?",
  ]}
>
Repartí `caramelos` en partes iguales entre `chicos` y mostrá el resultado con este formato exacto:

```
3 para cada uno, sobran 1
```
</EjercicioPython>

## Operaciones con textos

Los strings traen un montón de herramientas incorporadas. Estas son las que más vas a usar:

```python
frase = "  Hola Mundo  "

len(frase)              # 14   cuántos caracteres tiene (los espacios cuentan)
frase.strip()           # "Hola Mundo"     saca los espacios de los bordes
frase.upper()           # "  HOLA MUNDO  " todo en mayúsculas
frase.lower()           # "  hola mundo  " todo en minúsculas
frase.count("o")        # 2    cuántas veces aparece
frase.replace("o", "0") # "  H0la Mund0  "
```

:::caution[Los métodos NO modifican el original]
```python
nombre = "ana"
nombre.upper()
print(nombre)      # ana  ← ¡sigue igual!

nombre = nombre.upper()
print(nombre)      # ANA  ← ahora sí
```
Los strings son **inmutables**: `upper()` no cambia el texto, **devuelve uno nuevo**. Si no lo
guardás, se pierde. Es de los errores más comunes al empezar.
:::

### Partir y juntar

:::note[Antes: ¿qué es una "lista"?]
Acá aparece una palabra nueva. Una **lista** es simplemente **varios valores guardados juntos**, en
orden, entre corchetes: `["lunes", "martes"]`. Por ahora con eso alcanza — más adelante hay una clase
entera dedicada a ellas.
:::

```python
dias = "lunes,martes,miércoles"
dias.split(",")            # ['lunes', 'martes', 'miércoles']   texto → lista

palabras = ["Python", "es", "genial"]
" ".join(palabras)         # "Python es genial"                 lista → texto
```

`split` y `join` son opuestos, y van a aparecer todo el tiempo cuando leamos archivos.

:::note[El `join` se lee al revés de lo que parece]
Se escribe `separador.join(lista)`, no `lista.join(separador)`. El separador va adelante porque es
**él** el que se mete entre los elementos.
:::

### Preguntarle cosas a un texto

```python
frase = "Python es genial"

frase.startswith("Py")     # True    ¿empieza con...?
frase.endswith("al")       # True    ¿termina con...?
"es" in frase              # True    ¿está adentro?
```



## Más práctica

<EjercicioPython
  titulo="El tipo de cada dato"
  dificultad="🌱"
  datos={`nombre = "Maxi"\nedad = 22\naltura = 1.81\nes_estudiante = True`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr().strip().splitlines()[-4:]\nassert r == ["str", "int", "float", "bool"], f"esperaba str/int/float/bool y salió {r!r}"\nr2 = correr(edad=1.5).strip().splitlines()[-3:-2]\nassert r2 == ["float"], "si edad cambia a 1.5, el tipo tiene que decir float: no lo escribas a mano"`}
  pistas={[
    "Probá <code>print(type(nombre))</code> tal cual y mirá qué imprime. ¿Es exactamente lo que pide el test?",
    "<code>type()</code> devuelve algo como <code>&lt;class 'str'&gt;</code>. ¿Cómo sacás de ahí solamente el nombre? Probá con <code>.__name__</code>.",
  ]}
>
Mostrá el **tipo** de cada variable, uno por línea y en este orden: `nombre`, `edad`, `altura`,
`es_estudiante`.

Tiene que salir así:

```
str
int
float
bool
```
</EjercicioPython>

<EjercicioPython
  titulo="Todas las operaciones"
  dificultad="🌱"
  datos={`a = 10\nb = 3`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr(a=10, b=3).strip().splitlines()[-5:]\nassert r == ["13", "7", "30", "3.3333333333333335", "1"], f"salió {r!r}"\nr2 = correr(a=8, b=2).strip().splitlines()[-5:]\nassert r2 == ["10", "6", "16", "4.0", "0"], "con otros números tiene que acompañar"`}
  pistas={[
    "Son cinco cuentas distintas sobre los mismos dos números. ¿Cuál de los operadores da el resto?",
    "Fijate que la división da un número con decimales aunque los dos sean enteros. ¿Qué operador estás usando?",
  ]}
>
Mostrá, una por línea, la **suma**, la **resta**, la **multiplicación**, la **división** y el
**resto** entre `a` y `b`, en ese orden.
</EjercicioPython>

<EjercicioPython
  titulo="¿Es par?"
  dificultad="🌱"
  datos={`n = 8`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(n=8).strip().splitlines()[-1] == "True", f"con 8 salió {correr(n=8).strip().splitlines()[-1]!r}"\nassert correr(n=7).strip().splitlines()[-1] == "False", "7 no es par"\nassert correr(n=0).strip().splitlines()[-1] == "True", "el 0 es par"\nassert correr(n=-4).strip().splitlines()[-1] == "True", "los negativos también pueden ser pares"`}
  pistas={[
    "¿Qué tiene que dar el resto de dividir por 2 para que el número sea par?",
    "Acá no hace falta ningún <code>if</code>: la comparación <em>ya es</em> True o False. ¿Qué pasa si la imprimís directamente?",
  ]}
>
Mostrá `True` o `False` según si `n` es par. **Sin usar `if`**: la comparación ya vale por sí sola.
</EjercicioPython>

<EjercicioPython
  titulo="Una condición compuesta"
  dificultad="🌱"
  datos={`edad = 20\nes_estudiante = True`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(edad=20, es_estudiante=True).strip().splitlines()[-1] == "True", "20 años y estudiante"\nassert correr(edad=20, es_estudiante=False).strip().splitlines()[-1] == "False", "no es estudiante"\nassert correr(edad=17, es_estudiante=True).strip().splitlines()[-1] == "False", "17 no es mayor a 17"\nassert correr(edad=18, es_estudiante=True).strip().splitlines()[-1] == "True", "18 sí es mayor a 17"`}
  pistas={[
    "Mirá el test del 17: tiene que dar False. ¿<em>Mayor a 17</em> incluye al 17 o no?",
    "¿Alcanza con que se cumpla una de las dos condiciones, o hacen falta las dos?",
  ]}
>
Mostrá `True` solo si la edad es **mayor a 17** **y** además es estudiante.
</EjercicioPython>

<EjercicioPython
  titulo="Medir y transformar"
  dificultad="🌱"
  datos={`nombre = "Ana Lopez"`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`r = correr(nombre="Ana Lopez").strip().splitlines()[-3:]\nassert r == ["9", "ANA LOPEZ", "ana lopez"], f"salió {r!r}"\nr2 = correr(nombre="Tux").strip().splitlines()[-3:]\nassert r2 == ["3", "TUX", "tux"], "con otro nombre tiene que acompañar"`}
  pistas={[
    "Los espacios también son caracteres. ¿Los estás contando?",
    "¿Los métodos que pasan a mayúsculas modifican la variable original, o devuelven un texto nuevo?",
  ]}
>
Mostrá, una por línea: la **cantidad de caracteres** de `nombre`, el nombre en **mayúsculas** y en
**minúsculas**.
</EjercicioPython>

<EjercicioPython
  titulo="Contar una letra"
  dificultad="🌱"
  datos={`frase = "hola mundo maravilloso"\nletra = "o"`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(frase="hola mundo maravilloso", letra="o").strip().splitlines()[-1] == "4", "la o aparece 4 veces"\nassert correr(frase="hola mundo maravilloso", letra="a").strip().splitlines()[-1] == "3", "hay 3 letras a"\nassert correr(frase="abc", letra="z").strip().splitlines()[-1] == "0", "si no aparece, es 0"`}
  pistas={[
    "Los strings tienen un método que hace justo esto. ¿Cuál de los que viste cuenta apariciones?",
  ]}
>
Mostrá **cuántas veces** aparece `letra` dentro de `frase`.
</EjercicioPython>

<EjercicioPython
  titulo="Separar y unir"
  dificultad="🌿"
  datos={`dias = "lunes,martes,miércoles"`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr().strip().splitlines()[-1] == "lunes | martes | miércoles", f"salió {correr().strip().splitlines()[-1]!r}"\nassert correr(dias="a,b").strip().splitlines()[-1] == "a | b", "con otra lista tiene que acompañar"\nassert correr(dias="solo").strip().splitlines()[-1] == "solo", "con un solo día no hay separador"`}
  pistas={[
    "Son dos pasos: primero partir el texto por las comas, después volver a unirlo con otro separador.",
    "El separador va adelante del <code>join</code>, no la lista. ¿Cuál sería el separador exacto acá, contando los espacios?",
  ]}
>
`dias` es un texto con días separados por comas. Mostralo con ` | ` (espacio, barra, espacio) en
lugar de las comas:

```
lunes | martes | miércoles
```
</EjercicioPython>

<EjercicioPython
  titulo="Empieza y termina"
  dificultad="🌿"
  datos={`frase = "Python es genial"`}
  starter={`# Escribí tu programa acá.\n`}
  tests={`assert correr(frase="Python es genial").strip().splitlines()[-1] == "True", "empieza con Py y termina con al"\nassert correr(frase="Java es genial").strip().splitlines()[-1] == "False", "no empieza con Py"\nassert correr(frase="Python es lindo").strip().splitlines()[-1] == "False", "no termina con al"\nassert correr(frase="Pyal").strip().splitlines()[-1] == "True", "empieza con Py y termina con al"`}
  pistas={[
    "Son dos preguntas distintas sobre el mismo texto. ¿Qué método usás para cada punta?",
    "Los dos tienen que cumplirse a la vez para que dé True. ¿Con qué los unís?",
  ]}
>
Mostrá `True` solo si `frase` **empieza** con `Py` **y** **termina** con `al`.
</EjercicioPython>

---

Siguiente: `input()` y la entrada de datos — ojo que esa clase se practica en VS Code, porque acá el
navegador no tiene teclado para el programa. Mientras tanto, en estos ejercicios los datos entran
**por parámetro**, que es la forma que vas a usar el resto del curso.
