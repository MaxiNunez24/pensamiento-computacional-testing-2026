# MkDocs — sitio generado (gh-deploy lo maneja en gh-pages)
site/

# Astro (migración a la plataforma interactiva — rama dev)
node_modules/
dist/
.astro/
.env
.env.production

# Claude Code — configuración local de esta máquina
.claude/settings.local.json
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Editores
.vscode/
.idea/
*.swp

# Capturas del proceso de carga al SiGeS: son pantallas reales del sistema del
# Ministerio, con DNI y nombres de personas. El repo es PUBLICO: no se suben.
# Viven solo en la maquina del profe.
material-privado/capturas_bot/
